# TODO / 로드맵

이 문서는 **앞으로 해야 할 작업**만 담습니다. 끝난 작업의 기록(겪은 함정·설계 판단 근거 포함)은 `Completed.md`로 옮겼습니다.
게임 기획은 `README.md`, 코드 작업 규칙·현재 코드 상태는 `CLAUDE.md`,
**적용된 계산식과 밸런싱 기준은 `SystemFormulaBalance.md`**, 성장 곡선 분석은 `BalanceCurve.md`, UI 비주얼 규칙은 `UI_DesignReference.md`에 있습니다.

> 우선순위: 🔴 높음(핵심 재미·구조) / 🟡 중간 / 🟢 낮음(폴리시·후순위)
> 상태: 미착수 / 진행중 / 부분구현

**전체 세로 슬라이스는 이미 작동합니다.** 무엇이 되어 있는지는 `Completed.md`, 코드가 지금 어떻게 생겼는지는 `CLAUDE.md`를 보세요.

---

## 🔴 1순위 — 캐릭터 스킬 시스템

> 지금 캐릭터는 전부 기본 공격만 하고 개성은 스탯·시너지로만 표현된다. 이 묶음이 들어가면 캐릭터 선택이
> "스탯이 몇인가"에서 "무엇을 할 수 있는가"로 바뀐다.
>
> **순서: 1-1(데이터·규칙) → 1-2(스킬 버튼) → 1-3(도발) → 1-4(몬스터 정보) → 1-5(스킬 설명 UI).**
> 1-1이 없으면 나머지가 보여줄 것도 쓸 것도 없으므로 반드시 먼저 한다. 1-4는 앞과 독립적이라 순서를 바꿔도 된다.

### 1-1. 고유 스킬 데이터 + 사용 규칙  (미착수)
- **캐릭터마다 스킬 1개**(중복 보유 없음). 액티브/패시브 두 종류이며 캐릭터별로 다르다
- `SkillSO`는 **유닛 종류에 묶이지 않은 별도 에셋**이라 `CharacterStatsSO`에 참조 필드 하나만 더하면 상태이상·쿨타임 코드가 그대로 재사용된다(설계 당시 의도한 확장 지점)
- ⬜ **액티브/패시브 구분이 `SkillSO`에 없다** — 지금은 전부 "쿨타임 돌고 발동하는 액티브" 전제다. 패시브(상시 적용)는 발동 시점 자체가 없으므로 종류 필드 + 적용 경로를 새로 정해야 한다
  - 패시브를 스탯 보정으로 표현하면 **파티 시너지와 같은 자리를 건드린다**(`Unit.Stats`를 스냅샷/복원하는 `PartySynergyTracker`). 어느 쪽이 먼저 얹히는지, 되돌릴 때 서로를 덮지 않는지 먼저 정할 것
- ⬜ **플레이어의 스킬 사용 경로가 없다.** `PlayerActionSelector`는 지금 타겟만 받아 `ActionKind.Attack`을 만든다 — 스킬 사용 여부를 함께 받아 `ActionKind.Skill`을 만들 수 있어야 한다
- `Unit.IsSkillReady`/`ConsumeSkill`/`TickCooldown`은 **몬스터용으로 이미 다 있다**. 플레이어도 그대로 쓰면 된다
- ⚠️ 남은 쿨타임(`Unit._skillCooldownRemaining`)이 **private**이라 화면에 표기하려면 노출이 필요하다(1-2에서 쓴다)

### 1-2. 스킬 버튼 (우하단)  (미착수)
- 현재 행동 중인 캐릭터의 스킬을 화면 **우하단에 버튼**으로 띄운다
- **액티브**: 버튼을 누르면 이번 턴에 그 스킬을 쓴다. 켜짐/꺼짐은 **opacity로 구분**해 지금 스킬을 쓸 상태인지 한눈에 보이게 한다
- **쿨타임 중**: 아이콘 위에 **검은 배경 + 남은 턴 수 텍스트**를 덮는다
- 토글은 **버튼 클릭 또는 Z키** 둘 다로 켜고 끌 수 있다
- ⚠️ **Z키는 `Systems/InputControls.cs`에 액션을 추가해야 한다** — 액션 경로 문자열의 유일한 출처이고, `.inputactions` 에셋의 Battle 맵에도 같은 이름으로 넣어야 한다. 한쪽만 고치면 **키 설정 UI만 조용히 망가진다**(과거 사례)
  - 리바인딩 대상(`InputControls.Rebindable`)에 넣을지 함께 정할 것 — 넣으면 옵션 팝업 키설정에 행이 하나 늘어난다
- ⚠️ 배틀 입력이므로 **퍼즈 게이트(`IsGameplayInputEnabled`)를 타야 한다** — 퍼즈 중에 스킬이 토글되면 안 된다
- 위치는 HUD(`BattleHUD.uxml`)이며 갱신 시점은 **플레이어 차례 시작**이다(`ActorTurnStarted`). 기절한 유닛은 이 이벤트를 내지 않는다는 점에 주의

### 1-3. 도발(Taunt) 상태이상  (미착수)
- 걸린 몬스터는 그 턴에 **시전자만** 공격한다. 끼어들 자리는 `MonsterAiSelector.PickTarget` **한 곳뿐**이다(어그로 추첨 앞에 분기)
- **광역기(Line)는 애초에 그 함수를 타지 않으므로** "도발에 걸려도 광역기는 아군 전체가 맞는다"는 규칙이 분기 없이 성립한다
- ⚠️ **`ActiveStatus`에 시전자 Id 필드가 필요하다** — 지금은 Kind/Magnitude/RemainingTurns뿐이라 "누가 도발했는지"를 담을 곳이 없다. 도발에만 의미가 있는 필드라는 점을 주석으로 남길 것
- 아이콘은 `Textures/Icons/Debuff/`에 추가하고 **`Debuff_AttackDown.asset`의 Fallback 체인**에 등록해야 한다(다른 에셋에 걸면 조용히 무시된다)

### 1-4. 몬스터 정보 팝업 (Tab)  (미착수)
- Tab을 누르면 현재 웨이브의 몬스터 정보를 띄운다 — **이름 · 아이콘 · 스탯**, 스킬이 있으면 **스킬과 설명**까지
- 스킬 설명에는 **어떤 디버프(아이콘)를 몇 % 확률로 거는지**를 명시한다
- ⚠️ **표기할 확률은 `SkillSO.ApplyChance`(기본 확률)다.** 실제 부여 확률은 `ApplyChance × (1 − 대상 RES)`라 **맞는 대상마다 다르므로** 팝업에 "이 확률로 걸린다"고 단정하면 거짓말이 된다. 기본 확률임을 문구로 드러내거나, 선택한 파티원 기준 실효 확률을 따로 보여줄 것
- 아이콘은 체력바(TMP 스프라이트)와 달리 **UI Toolkit이라 `Textures/Icons/Debuff/`의 PNG를 `background-image`로 그대로 쓴다** — Sprite Asset·Fallback 체인과 무관하다
- 퍼즈 팝업(`BattlePausePanel`)과 같은 `BasePanelUI` + `RootElementName` 오버라이드 패턴. **퍼즈와 달리 전투를 멈추지 않는 단순 조회 창**인지, 여는 동안 입력을 막을지 먼저 정할 것
- Tab 키도 1-2의 Z키와 같은 경로(`InputControls` + `.inputactions`)로 추가한다

### 1-5. 스킬 설명 UI  (1-1~1-4 이후)
- 스킬이 무엇을 하는지 문장으로 풀어 보여주는 화면. **위 작업이 끝난 뒤에 착수**한다
- 문구는 `choice.<카테고리>.desc` 선례대로 **키 기반**으로 두는 편이 낫다 — 배율·확률 같은 수치를 문장에 박으면 밸런싱할 때마다 번역까지 고쳐야 한다

---

## 🟡 2순위 — UI / UX

### 2-1. 타겟팅 피드백  (부분구현)
- 마우스 2단계 클릭 + 방향키 순환/Enter·Space 확정은 완료. 겨냥된 **단일** 대상은 빨강 아웃라인 표시
- ⬜ 남은 것: 플레이어 차례에 **유효 대상 전체** 하이라이트 / 커서 피드백(어디를 겨냥할 수 있는지)
- `UnitView.SetOutlineLayer`/`ResetOutlineLayer`가 이미 있으므로 겨냥용(빨강)과 다른 레이어를 하나 더 두면 되는 구조

### 2-2. UI 비주얼 폴리시  (부분구현)
- ⬜ **캐릭터 선택 화면**: nav-arrow·인디케이터 점·스탯 바 강조색이 아직 구 시안/네이비 톤 — 새 민트/바이올렛 팔레트로 이전 필요
- ⬜ **버튼 크기**: `.btn` 계열이 레퍼런스 이미지 대비 작다. 톤(색상)은 유지하고 크기만 키우는 방향(레퍼런스 이미지 별도 공유 예정)
- 톤 전면 교체와 아이콘 작업은 완료 — `Completed.md` 4-4 참고

### 2-3. 배틀 중 정보 표시 확장  (필요성 재검토)
- 하단 파티 스탯·시너지 패널·화면 배치 인덱스는 완료(`Completed.md` 4-5)
- 스킬 쿨타임 표기와 몬스터 정보 조회는 **1-2·1-4로 옮겼다**(스킬 시스템과 함께 만드는 게 자연스럽다)
- ⬜ 남은 후보는 **다음 행동 예고** 정도. **정말 필요한지 판단부터** 하고 착수할 것

---

## 🟢 3순위 — 연출 / 폴리시

### 3-1. 연출 수치 조정  (인스펙터 값만)
- 근접 이동: `_approachDistance`(1.5) 모델이 겹치지 않는지, `_approachJumpHeight`(0.8) 궤적이 자연스러운지, `_approachDuration`(0.25) 페이싱이 늘어지지 않는지
  - 참고: 왕복이 연출 대기에 포함돼 **공격 1회당 0.5초**가 더해진다. 늘어지면 `_approachDuration`부터 줄일 것
- 투사체: `ProjectileSpawner._speed`(18) / `_arcHeight`(0이면 직선) / `_muzzleLeadSeconds`(0.05)

### 3-2. 남은 이펙트 배정  (선택)
- `_hitEffect`가 비어 있는 유닛 3종(Knight / Rogue_Dagger / Skeleton_Minion) — 전역 기본값으로 떨어지므로 **동작은 정상**이다. 무기별 타격감을 주려면 채울 것
- `_muzzleFlash`는 근접 4종에 비어 있다(총구가 없는 무기라 자연스러움 — 필요 없으면 그대로 둘 것)
- 점프/이동 클립을 만들어 애니메이터에 추가하면 `MoveAsync`에서 트리거만 같이 재생하면 된다(지금도 동작하므로 급하지 않음)

---

## 🔧 기술 부채 / 알려진 이슈

- **에셋 이름 오타**: `ScriptableObjects/Skill/Skill_SkeltonWarrior.asset` — `Skeleton`이 `Skelton`으로 빠져 있다. 참조가 늘기 전에 고치는 편이 낫다
- **미사용 USS 변수**: `Common.uss`의 `--color-cyan-mid`가 버튼 팔레트 교체 이후 실사용처 없음. 반면 `--color-cyan-light`는 시너지 패널의 `×N`에서 쓰이므로 **남겨둘 것**(캐릭터 선택 잔여 시안 톤과 함께 지우지 말 것)
- **`MinionSO.asset`에 구 필드 잔여**: `_skillCooldown`/`_skillScope`/`_skillPowerMultiplier` — `SkillSO` 분리 이전의 인라인 필드가 YAML에 남아 있다. Unity가 무시하므로 무해하고, 에디터에서 한 번 수정·저장하면 사라진다
- **Core 유닛 테스트 미도입**: asmdef 분리로 전제조건은 해결됐고 `com.unity.test-framework`도 설치돼 있으나 테스트 어셈블리가 없다. 회귀 가치가 높은 후보 — `DamageCalculator`(비율 감소+크리), `TurnOrder`(SPD 정렬), `StageScaling.CreatePlayerGrowth`(반올림 누적 방지), `Unit.TryApplyStatus`(RES 저항 확률), `RunSnapshot`(캡처→복원 왕복)
- **패널 폴더 정리 미완**: "배틀 패널 3종을 `Battle/View/Panels/`로 모은다" 중 `BasePanelUI` 상속만 적용됐고 폴더 이동은 안 했다(현재 `Battle/View/` 직속). 순수 정리 작업이라 우선순위 낮음
- **UXML `<Style src>` 링크**: 에디터 밖에서 파일을 생성하면 임포트 순서 때문에 USS 링크가 안 걸릴 수 있음 → 해당 UXML Reimport(또는 에디터 재시작)로 해결. **신규 UI 추가 시 주의**
- **BattleScene 직접 실행 시 씬 전환 없음**: `GameManager`가 없어 `LoadScene`이 조용히 건너뛰어진다(결과 화면까지는 정상). 테스트 파티 폴백과 짝을 이루는 의도된 동작이지만 로그가 없어 원인 파악이 늦어질 수 있음
- **연출 시간 하드코딩**: `UnitView`의 spawn/attack/skill/hit/die 지속시간이 인스펙터 수동 값. 애니 클립 길이 자동 추출을 고려할 수 있음(선택)
- **세이브 버전 마이그레이션**: `SaveData.Version`을 올릴 일이 생기면(스탯 리스케일 등) **이어하기 스냅샷(`data.Run`)은 버리는 편이 안전**하다 — 옛 수치가 그대로 되살아난다. `SaveService.Normalize`에 주석으로 남겨둠

---

## ⚖️ 밸런싱 (TBD — 별도 패스)

기획·구현과 분리해 수치만 조정하는 단계. 전부 SO 데이터라 코드 수정 없이 조정 가능하다.
**지금까지의 조정 이력(1·2차 + 시너지 + 10배 리스케일)은 `Completed.md` 9절**, 분석 근거는 `BalanceCurve.md`에 있다.

### 남은 항목 (BalanceCurve.md 결론 순서)

1. ⬜ **파티 인원이 난이도를 지배한다** — 솔로 안전율 1.1~1.6 vs 4인 11~22. 난이도 최저점은 솔로 5~7스테이지.
   손잡이는 `Recruit.asset`의 `_weightPerEmptySlot`(현재 4)이지만, **"4인 파티가 정상 상태인가"를 먼저 정해야** 방향이 나온다
2. ⬜ **복리 대응(선택지 % 전환)** — 2차 조정으로 균형 플레이가 50 전후에서 막히도록 맞춰졌다. 그보다 더 멀리 보낼 생각이면 그때 검토
3. ⬜ **% 전환 시 8%는 과하다**(50스테이지 128.5배). 3~4%부터 대조표로 재확인

### ⚠️ 조정 전에 반드시 볼 것 — 스탯마다 같은 %의 가치가 다르다

| 스탯 | 실제 효과 | 조정 감각 |
|---|---|---|
| ATK | 피해량에 **선형** 반영 | +20% = 피해 +20% |
| 치명타/치명피해 | 기대 피해 배율에 반영 | 현재 크리율에 따라 달라짐 |
| DEF | `K/(K+DEF)` 감쇠라 **수익 급감** | DEF 150→300(+100%)이 받는 피해 −11.5%뿐 |
| SPD | 모든 유닛이 턴당 1회 행동 → **순서만** 바뀜 | 전투력 환산 가치가 가장 낮음 |

비율 필드는 `Range(0, 2)`라 DEF처럼 큰 값이 필요한 스탯도 표현할 수 있다.

- ⚠️ **1·2차 조정 모두 모델 계산 기반이고 실제 플레이 검증은 아직이다.** 특히 방어·속도는 저항·턴 순서를 모델이 단순화해 다뤄 오차가 크므로, 플레이에서 과하게 느껴지면 그 둘부터 내릴 것
- ⚠️ **난이도를 잴 때 표본 스테이지가 5의 배수(보스)에 몰리지 않게 할 것** — 1차 조정이 실제로 그 오류를 겪었다
- ⚠️ **`DefenseConstant`는 스탯 스케일과 함께 움직인다** — 스케일을 다시 바꿀 일이 생기면 이 상수를 가장 먼저 확인

### 조정 대상 목록
- 캐릭터/몬스터 세부 스탯 (`CharacterStatsSO`/`MonsterStatsSO`)
- 로그라이크 선택지 수치 및 **카테고리별 등장 가중치** (`RoguelikeChoiceSO`)
- 데미지 공식 상수 (`DamageCalculator.DefenseConstant` 등)
- 스폰 패턴 구성 (`SpawnWaveSO` / 패턴 풀)
- 보스/엘리트 스탯 배율
- 영구 포인트 획득 간격 (`PointAllocationPopupUI._stagesPerPoint`), 포인트당 가중치 (`RoguelikeRewardService._weightPerPoint`)

---

## 📝 기타 미정 (README TBD)

- 게임 타이틀(제목)
- 모바일 확장 여부
- ~~다국어 지원 여부~~ → **한국어/영어 2종으로 확정(2026-08-10)**. 언어를 늘리려면 `LanguageCode`에 항목을 더하고 `LocalizationTableSO.Entry`에 열을 추가한 뒤 `OptionPopupUI.LanguageOrder`에 버튼을 넣으면 된다
