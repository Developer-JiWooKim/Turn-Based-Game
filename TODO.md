# TODO / 로드맵

이 문서는 앞으로 해야 할 작업을 정리한 목록입니다.
게임 기획은 `README.md`, 코드 작업 규칙·현재 코드 상태는 `CLAUDE.md`를 참고하세요.

> 우선순위 표기: 🔴 높음(핵심 재미·구조) / 🟡 중간 / 🟢 낮음(폴리시·후순위)
> 상태 표기: 미착수 / 진행중 / 부분구현

---

## ✅ 지금까지 완료 (검증됨)

전체 세로 슬라이스가 실제 Unity에서 작동함:
- **씬 흐름**: IntroScene(타이틀 → 캐릭터 선택) → BattleScene(전투) → 승리 시 다음 스테이지 / 전멸 시 IntroScene 복귀
- **전투**: Core(턴 순서·데미지·크리·보스 스킬·이벤트) ↔ View(스폰·애니·체력바·클릭 타겟팅·카메라 쉐이크)
- **런/진행**: RunData(파티·스테이지), 캐릭터 선택 → 전투 반영, 웨이브별 스테이지 루프
- **HUD**: 스테이지·턴 순서·현재 행동 유닛·플레이어 차례 프롬프트
- **로그라이크(간소화)**: 승리 시 3개 중 1개 선택 → 런 지속 강화 5종(공격력/스피드/방어형/크리/회복) 적용

---

## 🔴 1순위 — 핵심 시스템 완성

### 1-1. 로그라이크 선택지 나머지 4종  (상태: 부분구현)
현재 5종만 구현(런 지속 강화). README 기준 아래가 미구현:
- **몬스터 행동불가**: 다음 스테이지 몬스터 전체 1턴 행동 불가 (일회성, 강효과)
- **몬스터 체력감소**: 다음 스테이지 몬스터 HP 감소 (일회성)
- **몬스터 공격력감소**: 다음 스테이지 몬스터 ATK 감소 (일회성)
- **파티원 영입**: 6종 중 랜덤 등장(중복 허용), 여유 있으면 즉시 합류 / 4명 초과 시 교체 대상 선택
- 구조 메모: 몬스터 디버프 계열은 "다음 스테이지 스폰 시 1회 적용되는 예약 효과"라 `RunData`에 **다음 웨이브에 적용할 pending 디버프**를 담는 방식이 필요. 현재 `RoguelikeReward`는 파티 즉시 적용만 지원 → 몬스터 대상/예약 효과를 표현하도록 확장 필요

### 1-2. 파티 확장 (최대 4명)  (상태: 미착수)
- 현재 파티는 1명 고정. 영입(1-1) 구현과 맞물림
- BattleDirector `_playerSlots`가 4개까지 대응하는지, 배치·타겟(아군은 클릭 대상 아님) 확인
- 4명 초과 영입 시 교체 UI 필요

### 1-3. 스폰 패턴 (6스테이지 이후 / 보스)  (상태: 부분구현)
- 현재 `SpawnWaveSO` 배열을 순서대로 순환하는 수준
- README: **1~5스테이지 수동 설계**(현 방식 유지) / **6스테이지+ 정해진 패턴 풀에서 랜덤** / **5의 배수 스테이지는 보스 패턴**
- 스폰 패턴 풀 SO + 스테이지 규칙(랜덤/보스 분기) 구현 필요

### 1-4. 보스 실체화  (상태: 부분구현)
- 애니메이터에 `Skill` 스테이트는 있으나 **현재 Attack과 같은 클립**(임시). 보스 전용 스킬 애니 클립 준비 시 교체
- 보스 프리팹/`MonsterStatsSO`(Tier=Boss, HasSkill) 실제 세팅
- 보스 스킬 연출(단일/라인) 이펙트

---

## 🟡 2순위 — 메타 진행 / 성장

### 2-1. 성향 포인트 배분 팝업  (상태: 미착수)
- 캐릭터 선택 완료 후(SelectWeapon 단계) 등장
- 로그라이크 카테고리별 **등장 가중치**에 영구 포인트 투자 (성향 커스터마이징)
- 무료 리스펙(초기화) 지원

### 2-2. 영구 포인트 시스템  (상태: 미착수)
- 10스테이지 단위 최초 돌파 시 영구 포인트 1점 획득
- 위 2-1 가중치 투자에 사용

### 2-3. 로컬 영구 저장  (상태: 미착수)
- 최고 도달 스테이지 저장/로드 (PlayerPrefs 또는 JSON)
- 영구 포인트/가중치 배분도 저장

### 2-4. 파티 시너지  (상태: 미착수)
- 동일 캐릭터(같은 무기 포함) 2명↑ 시 유형별 추가 효과
- 조건 깨지면(다운/추방) 즉시 소멸
- 유형별 구체 효과는 TBD

---

## 🟡 3순위 — UI / UX

### 3-1. 결과 화면 팝업  (상태: 미착수)
- 현재 승리/패배는 로그 + 씬전환만
- 패배(리타이어) 시 결과 화면(도달 스테이지 등) 후 타이틀 복귀
- 승리 연출(선택지 팝업 전/후) 정리

### 3-2. 타겟팅 피드백  (상태: 미착수, `TargetingController` 내 TODO)
- 플레이어 차례에 유효 대상(몬스터) 하이라이트 / 커서 피드백
- 현재는 감으로 클릭

### 3-3. 캐릭터 선택 화면 폴리시  (상태: 부분구현)
- `stat-placeholder`에 실제 스탯바 표시(현재 자리표시자)
- 무기 아이콘 등 비주얼

### 3-4. 옵션 메뉴 기능화  (상태: 부분구현 — 팝업만 존재, 기능 확인 필요)
- 사운드 볼륨 조절
- 해상도(창모드 3종 + 전체화면)
- 언어(기본 영어) — 다국어 지원은 완성 후 결정

---

## 🟢 4순위 — 연출 / 사운드 / 폴리시

### 4-1. 히트 이펙트 / 파티클  (상태: 미착수)
- 타격 시 히트 이펙트 재생 (외부 에셋 구매/다운로드 예정)
- `BattleDirector.PlayActionAsync`에 이펙트 스폰 훅 추가 지점 있음

### 4-2. 사운드 / BGM  (상태: 미착수)
- 전투 타격음, UI 사운드, 배경음

### 4-3. 입력 시스템 정식화  (상태: 미착수)
- `Assets/InputSystem_Actions.inputactions`가 Unity 기본 템플릿 그대로
- 현재 타겟팅은 `Mouse.current` 직접 사용 → 커맨드 배틀용 액션맵으로 정식 재정의

---

## 🔧 기술 부채 / 알려진 이슈

- **`Singleton<T>` 중복 처리 버그**: `Awake`의 `if (Instance != null && Instance == this)` 조건이 사실상 중복 인스턴스를 파괴하지 못함(→ `!= this`여야 함). 현재 GameManager가 IntroScene에만 있어 문제 표면화 안 됐지만, BattleScene 등에 또 두면 오작동 가능
- **UXML `<Style src>` 링크**: 에디터 밖에서 파일 생성 시 임포트 순서 때문에 USS 링크가 안 걸릴 수 있음 → 해당 UXML Reimport(또는 에디터 재시작)로 해결. 신규 UI 추가 시 주의
- **연출 시간 하드코딩**: `UnitView`의 spawn/attack/skill/hit/die 지속시간이 인스펙터 수동 값. 애니 클립 길이 자동 추출을 고려할 수 있음(선택)
- **밸런싱 수치 전반 TBD**: 아래 참고

---

## ⚖️ 밸런싱 (TBD — 별도 패스)

기획·구현과 분리해 수치만 조정하는 단계. 전부 SO 데이터로 관리 중이라 코드 수정 없이 조정 가능.
- 캐릭터/몬스터 세부 스탯 (`CharacterStatsSO`/`MonsterStatsSO`)
- 로그라이크 선택지 수치 및 **카테고리별 등장 가중치** (`RoguelikeChoiceSO`)
- 데미지 공식 상수 (`DamageCalculator.DefenseConstant` 등)
- 스폰 패턴 구성 (`SpawnWaveSO` / 패턴 풀)
- 보스/엘리트 스탯 배율

---

## 📝 기타 미정 (README TBD)

- 게임 타이틀(제목)
- 모바일 확장 여부
- 다국어 지원 여부
