# 시스템 공식 / 밸런스 정리

이 문서는 프로젝트에 **실제로 적용되어 있는** 계산식을 한곳에 모은 것입니다.
수치를 조정할 때 어디를 만져야 하는지, 무엇이 함께 움직여야 하는지를 판단하는 기준으로 씁니다.

- 코드 규칙·구조는 `CLAUDE.md`, 남은 작업은 `TODO.md`, 기획은 `README.md`를 참고하세요.
- **이 공식들로 실제 성장 곡선을 계산한 결과는 `BalanceCurve.md`에 있습니다** — 수치를 조정하기 전에 그쪽의 "결론" 절을 먼저 보세요.
- **밸런싱 수치는 전부 SO/인스펙터에 있고, 이 문서의 값은 2026-08-06 기준 스냅샷입니다.**

## 현재 상수 스냅샷

| 값 | 현재 | 위치 |
|---|---|---|
| DEF 감쇠 상수 `K` | 1000 | `DamageCalculator.DefenseConstant` (코드 상수) |
| 최소 데미지 | 1 | `DamageCalculator.MinimumDamage` (코드 상수) |
| 플레이어 성장률 HP/ATK/DEF | 0.05 / 0.04 / 0.03 | `StageScalingSO.asset` |
| 몬스터 성장률 HP/ATK/DEF | 0.08 / 0.06 / 0.03 | `StageScalingSO.asset` |
| 몬스터 복리 | ON | `StageScalingSO.asset` |
| 난이도 가속 시작 / 배수 | 30스테이지 / ×1.5 | `StageScalingSO.asset` |
| 몬스터 SPD 성장 / 시작 | 0.03 / 5스테이지 이후 | `StageScalingSO.asset` |
| 보스 처치 SPD 보상 | 기준 SPD의 0.08 | `StageScalingSO.asset` |
| 보스 스테이지 간격 | 5 | `MonsterSpawner._bossStageInterval` |
| 선택지 제시 개수 | 3 | `RoguelikeRewardService._choiceCount` |
| 영입 후보 수 | 3 | `RoguelikeRewardService._recruitCandidateCount` |
| 포인트 1점당 가중치 | 1 | `RoguelikeRewardService._weightPerPoint` |
| 포인트 획득 간격 | 1스테이지당 1점 | `PointAllocationPopupUI._stagesPerPoint` |

> **스탯 스케일**: 2026-08 정수 스탯(HP/ATK/SPD/DEF)을 전부 10배로 리스케일했습니다.
> 비율값(치명타/치명피해/저항/성장률/스킬 배율/상태이상 크기)은 스케일 대상이 아닙니다.

---

## 공식 이름: 데미지 계산 (비율 감소 + 치명타)

### 1. 이 공식이 필요한 이유
- 방어력을 뺄셈으로 처리하면 "ATK ≤ DEF"인 순간 피해가 0이 되어 전투가 멈춘다. 비율 감소는 방어력이 아무리 높아도 피해가 0이 되지 않으면서, 올릴수록 수익이 체감해 무한 성장을 자연스럽게 억제한다.

### 2. 입력값
- 값 1: 공격자 유효 ATK (`Unit.EffectiveAtk` — 상태이상 감소 반영)
- 값 2: 대상 유효 DEF (`Unit.EffectiveDef`)
- 값 3: 스킬 배율 `powerMultiplier` (일반 공격 1.0)
- 값 4: 공격자 `CritRate` / `CritDmg`, 난수

### 3. 출력값
- 정수 피해량과 치명타 여부 (`DamageResult`)

### 4. 공식
```
감쇠      = K / (K + 유효DEF)
기본피해  = 유효ATK × 감쇠 × 스킬배율
치명타    = rng.Value01() < CritRate
최종피해  = 치명타면 기본피해 × CritDmg, 아니면 기본피해
피해량    = max(1, Round(최종피해, AwayFromZero))
```
- 위치: `Battle/Core/DamageCalculator.cs`

### 5. 예시 계산
- 입력: Knight ATK 200 → Minion DEF 80, 일반 공격, 비치명타
- 계산: `1000 / (1000 + 80) = 0.9259` → `200 × 0.9259 × 1.0 = 185.19`
- 결과: **185** (치명타였다면 `185.19 × 1.5 = 277.8` → **278**)

### 6. 상한/하한
- 최소값: 1 (`MinimumDamage`)
- 최대값: 없음

### 7. 밸런스 의도
- 감쇠 상수 `K`는 **"피해가 절반으로 줄어드는 DEF 값"**이다. `K = 1000`이므로 DEF 1000에서 피해가 50%가 된다. 현재 캐릭터 DEF가 10~200 구간이라 감쇠는 0.83~0.99 사이 — **방어력의 영향이 의도적으로 완만하다.**
- 치명타는 확률 × 배율이라 기대 피해 배율은 `(1 − CritRate) + CritRate × CritDmg`. Rogue_Dagger(0.5 / 2.5)는 기대 1.75배, Knight(0.25 / 1.5)는 1.125배.

### 8. 조심할 점
- ⚠️ **`K`는 스탯 스케일과 함께 움직여야 한다.** 스탯만 10배 하고 `K`를 두면 방어력이 10배 세져 전투가 늘어진다(DEF 20 → 통과율 83%, DEF 200 → 33%). 10배 리스케일 때 100 → 1000으로 올린 이유다.
- 치명타 판정은 `Stats.CritRate`(원본)를 보고 데미지는 `EffectiveAtk`(감소 반영)를 본다 — 치명타를 깎는 상태이상은 아직 없다.
- 표시되는 피해량은 **클램프 전 값**이다. HP 20 남은 적을 300으로 때리면 화면에는 300이 뜬다(캐릭터의 실제 화력을 읽기 위한 확정 규칙).

---

## 공식 이름: 몬스터 스테이지 스케일링

### 1. 이 공식이 필요한 이유
- 무한 타워라 "언젠가는 못 이기는 벽"이 필요하다. 몬스터는 매 스테이지 새로 스폰되므로 현재 HP를 이어받을 걱정이 없어 배율을 그대로 곱할 수 있다.

### 2. 입력값
- 값 1: 몬스터 SO 기준 스탯 (MaxHp / Atk / Def)
- 값 2: 현재 스테이지 번호
- 값 3: 성장률과 복리 토글 (`StageScalingSO`)

### 3. 출력값
- 스폰 시점의 몬스터 `Stats` (치명타·저항은 변경 없음. **SPD는 2026-08-10부터 별도 규칙으로 증가**)

### 4. 공식
```
steps       = max(0, 스테이지 − 1)
normalSteps = min(steps, accelStartStage − 1)      # 가속 전 구간
accelSteps  = steps − normalSteps                  # 가속 구간
accelRate   = rate × accelMultiplier

배율 = 복리 ON  → (1 + rate)^normalSteps × (1 + accelRate)^accelSteps
       복리 OFF → 1 + rate × normalSteps + accelRate × accelSteps
결과 = max(1, Round(기준값 × 배율))

# SPD만 별도(가속 미적용)
spdSteps = max(0, 스테이지 − spdStartStage)
SPD      = max(1, Round(기준SPD × (1 + monsterSpdRate)^spdSteps))
```
- 위치: `Battle/Core/StageScaling.ApplyToMonster` / `Multiplier` / `ScaleSpd`
- **가속 구간을 나누는 이유**: 성장률만 통째로 올리면 초반까지 함께 어려워진다. "30스테이지까지는 편하게, 이후 빠르게"를 만들려면 구간을 끊어야 한다
- **SPD에 가속을 적용하지 않는 이유**: 가속 구간에서 속도까지 튀면 선공이 한순간에 뒤집혀 플레이어가 대응할 여지가 없다

### 5. 예시 계산
- 입력: Minion HP 500, 36스테이지, HP 성장률 0.08, 가속 시작 30 / 배수 1.5, 복리 ON
- 계산: `steps = 35`, `normalSteps = 29`, `accelSteps = 6`, `accelRate = 0.12`
- `1.08^29 = 9.317`, `1.12^6 = 1.974` → 배율 `18.39` → `500 × 18.39`
- 결과: **9195** (가속이 없었다면 `1.08^35 = 14.79` → 7395)

### 6. 상한/하한
- 최소값: 1 (0으로 떨어지지 않게)
- 최대값: 없음 — 복리라 스테이지가 오를수록 발산한다

### 7. 밸런스 의도
- **몬스터 성장률이 플레이어보다 높은 것이 전제다.** 플레이어는 로그라이크 선택지로 원하는 스탯을 몰아 올릴 수 있으므로, 자동 성장은 "완전히 뒤처지지 않을 정도"만 담당한다.
- 치명타·저항은 스케일링 대상이 아니다(곱하면 금방 상한에 붙는다).
- **SPD는 2026-08-10에 방침이 뒤집혔다.** 원래는 "몬스터만 올리면 선공을 계속 뺏겨 체감이 나쁘다"는 이유로 제외했는데, 그 결과 **속도 강화 선택지가 사실상 빈 선택지**가 됐다(SPD를 올릴 이유가 없으므로). 지금은 5스테이지 이후 몬스터 SPD를 3%씩 올려 선공 압박을 만들고, 보스 처치마다 플레이어에게 기준 SPD의 8%를 돌려준다. ⚠️ **보스 보상은 일부러 몬스터 증가분보다 작다** — 같은 속도로 주면 압박이 사라져 원래 문제로 되돌아간다.

### 8. 조심할 점
- ⚠️ **복리 토글이 난이도에 가장 크게 작용한다.** 난이도 조정은 이 값부터 만지는 게 효과가 크다.
- ⚠️ **난이도를 재는 표본 스테이지가 보스 배수(현재 5)에 몰리지 않게 할 것.** 보스 웨이브는 HP·DEF가 크게 달라 일반 웨이브와 결과가 다르다 — 실제로 1차 밸런싱이 10/20/30/40/50만 보고 진행돼 보스전 기준으로 맞춰진 적이 있다(`BalanceCurve.md` 참고).
- 플레이어와 증가 방식이 다르다(몬스터=지수, 플레이어=선형). 20스테이지 기준 몬스터 HP ×4.32 / 플레이어 HP ×1.95이며, 이 격차는 **선택지 성장이 메우도록 설계**돼 있다. 몬스터 성장률을 올리기 전에 선택지 수치를 먼저 볼 것.

---

## 공식 이름: 플레이어 스테이지 자동 성장 (누적 차분)

### 1. 이 공식이 필요한 이유
- 플레이어는 현재 HP를 스테이지 간 이어받으므로 배율을 곱하면 현재 HP와 최대 HP가 어긋난다. 그래서 flat 증가분을 영구 누적하는 방식을 쓴다.
- 매 스테이지 따로 반올림하면 오차가 쌓여 실제 비율에서 크게 벗어나므로, **누적 총량의 차분**을 돌려준다.

### 2. 입력값
- 값 1: 성장 전 기준 스탯 `BaseStats` (SO 원본)
- 값 2: 몇 번째 진급인지 `step`
- 값 3: 플레이어 성장률 (HP 0.05 / ATK 0.04 / DEF 0.03)

### 3. 출력값
- 이번 진급에서 더할 flat 증가분 (`RoguelikeEffect` 형태)

### 4. 공식
```
누적총량(step) = Round(기준값 × rate × step)
이번증가분     = 누적총량(step) − 누적총량(step − 1)
```
- 위치: `Battle/Core/StageScaling.CreatePlayerGrowth` / `StepGrowth` / `TotalGrowth`

### 5. 예시 계산
- 입력: Knight DEF 150, rate 0.03 (스텝당 4.5)
- 계산: 누적총량 = 4 / 9 / 14 → 차분 = **4, 5, 5**
- 결과: 3스텝 후 누적 **+14** (이론값 13.5에 근접). 매 스텝 따로 반올림했다면 `4+4+4 = 12`로 2만큼 낮게 굳었을 것

### 6. 상한/하한
- 최소값: 0 (rate가 0이거나 step이 0 이하면 0)
- 최대값: 없음

### 7. 밸런스 의도
- SPD는 자동 성장 대상이 아니다(0 고정) — 로그라이크 선택지로만 오른다.
- 성장의 기준이 **현재 스탯이 아니라 `BaseStats`**다. 그래서 선택지로 스탯을 몰아줘도 자동 성장분이 함께 부풀지 않는다.

### 8. 조심할 점
- ⚠️ **"매 스테이지 최소 +1" 같은 바닥값을 다시 넣지 말 것.** 과거에 그 바닥 때문에 저스탯 캐릭터가 비정상적으로 급성장한 전례가 있다. 10배 리스케일 이후에는 반올림 손실 자체가 작아져 바닥값이 필요하지도 않다.
- 영입 소급 성장이 이 함수를 **step 1부터 되짚어** 호출하므로(아래 항목), 여기 공식을 바꾸면 영입자 스탯도 함께 바뀐다.

---

## 공식 이름: 영입자 소급 성장 (Catch-up)

### 1. 이 공식이 필요한 이유
- 20스테이지에서 합류한 캐릭터가 1스테이지 기준 스탯 그대로면 즉시 전력 외가 된다. 지금까지 쌓였을 자동 성장을 되짚어 채워준다.

### 2. 입력값
- 값 1: 영입 대상 `CharacterStatsSO`의 기준 스탯
- 값 2: 현재 스테이지
- 값 3: 플레이어 성장률 (`StageScaling`)

### 3. 출력값
- 합류 시점의 `Stats` (현재 HP는 최대치로 채워짐)

### 4. 공식
```
for step = 1 .. (현재스테이지 − 1):
    ApplyStageGrowth( CreatePlayerGrowth(BaseStats, step) )
```
- 위치: `Progression/Run/RunData.ApplyCatchUp`

### 5. 예시 계산
- 입력: Barbarian ATK 270, rate 0.04, 11스테이지에서 영입 (steps 1~10)
- 계산: 누적총량(10) = `Round(270 × 0.04 × 10)` = 108
- 결과: ATK **378**로 합류

### 6. 상한/하한
- 최소값: 기준 스탯 그대로(1스테이지 영입)
- 최대값: 없음

### 7. 밸런스 의도
- ⚠️ **로그라이크 선택지 성장은 소급하지 않는다 — 이건 버그가 아니라 확정된 규칙이다.** "그건 그 시점 파티가 벌어들인 몫"이며, 파티를 살려두는 데 가치를 두기 위한 설계다. 그래서 새 영입자는 기존 파티원보다 약한 것이 정상이며, 하단 파티 스탯 표기에서 파란 `(+선택지)` 괄호가 새 영입자만 비어 보이는 것도 정상 결과다.

### 8. 조심할 점
- 영입 후보 카드의 미리보기(`PreviewRecruitStats`)가 **같은 함수를 공유**한다. 그래서 카드에 뜬 값과 실제 합류 결과가 어긋나지 않는다 — 한쪽만 고치지 말 것.
- 규칙을 바꾸려면(전부 소급 / 일정 비율만 소급) 밸런싱 결정이 먼저다.

---

## 공식 이름: 파티 시너지 (혼합 모델)

### 1. 이 공식이 필요한 이유
- 같은 캐릭터를 중복 영입하는 선택에 보상을 준다. 무한 타워라 **고정값 보너스는 후반에 필연적으로 무의미해지므로** 정수 스탯은 비율로 준다.

### 2. 입력값
- 값 1: 시너지 보유 캐릭터가 파티에 몇 명인지 vs `SynergyThreshold`
- 값 2: 시너지를 얹기 직전의 `Stats` (성장이 모두 반영된 값)
- 값 3: `CharacterStatsSO`의 시너지 수치 6종

### 3. 출력값
- 전투용 `Unit.Stats`에 얹힌 보너스 (전투 중에만 유효, 런 데이터에는 누적하지 않음)

### 4. 공식
```
ATK / SPD / DEF  : 스탯 += Round(스탯 × rate, AwayFromZero)   ← 비율
치명타 / 저항     : 스탯  = min(1, 스탯 + 가산치)              ← %p 가산 + 100% 클램프
치명피해          : 스탯 += 가산치                              ← %p 가산 (상한 없음)
```
- 위치: `Battle/Core/SynergyBonus.ApplyTo`

### 5. 예시 계산
- 입력: Knight 2명, DEF 150, `_synergyDefRate` 1.0(+100%)
- 계산: `150 + Round(150 × 1.0) = 300` → 감쇠가 `1000/1150 = 0.870`에서 `1000/1300 = 0.769`로
- 결과: DEF **300**, 받는 피해 **−11.6%** (실효 HP +13%)

### 6. 상한/하한
- 최소값: 0 (수치가 전부 0이면 시너지 없는 캐릭터로 취급 — `IsEmpty`)
- 최대값: 치명타·저항은 1.0(100%)로 클램프. ATK/SPD/DEF 비율은 인스펙터 `Range(0, 2)`

### 7. 밸런스 의도
- 현재 목표는 **효과 기준 +15~18%대 정렬**이다: Barbarian ATK +18% / Rogue_Dagger 치명타 +20%p / Mage 치명피해 +90%p / Knight DEF +100%·RES +15%p / Rogue_Crossbow ATK +12%·SPD +20% / Ranger SPD +50%·RES +15%p.
- 최대 HP는 시너지 대상이 아니다 — 조건이 깨지면 되돌려야 하는데 현재 HP가 따라오지 못한다.

### 8. 조심할 점
- ⚠️ **표시된 %를 스탯끼리 비교하면 안 된다.** ATK +20%는 피해 +20%지만, DEF +100%는 감쇠 때문에 받는 피해 −11.6%뿐이고, SPD는 턴 "순서"만 바꾸므로 전투력 환산 가치가 가장 낮다. 2026-08 조정 전에는 이 차이를 못 보고 Knight·Ranger를 과하다고 오판했지만 실제로 과했던 건 Barbarian이었다.
- 제거는 뺄셈이 아니라 **적용 전 스냅샷 복원**이다. 치명타·저항이 1.0으로 클램프되면 뺄셈은 오차를 남기기 때문.
- 비율 시너지는 **스탯 리스케일의 영향을 받지 않는다**(마이그레이션 불필요).

---

## 공식 이름: 상태이상 부여 확률 (RES 저항)

### 1. 이 공식이 필요한 이유
- `Stats.Res`가 소비되는 유일한 곳이다. 이 공식이 없으면 저항은 선택지·시너지로 값만 쌓이는 죽은 스탯이 된다.

### 2. 입력값
- 값 1: 스킬의 부여 확률 `StatusEffect.ApplyChance`
- 값 2: 대상의 `Stats.Res` (0~1로 클램프)
- 값 3: 난수

### 3. 출력값
- 부여 성공/실패 (bool)

### 4. 공식
```
최종확률 = ApplyChance × (1 − clamp(RES, 0, 1))
성공     = rng.Value01() < 최종확률
```
- 위치: `Battle/Core/Unit.TryApplyStatus`

### 5. 예시 계산
- 입력: Warrior 스킬 기절(확률 0.35) → Knight RES 0.2
- 계산: `0.35 × (1 − 0.2) = 0.28`
- 결과: **28%** 확률로 기절

### 6. 상한/하한
- 최소값: 0 (RES 1.0 = 완전 면역)
- 최대값: `ApplyChance` 그대로 (RES 0)

### 7. 밸런스 의도
- RES 100%를 완전 면역으로 두어 저항 투자에 명확한 목표점을 만든다.
- 같은 종류를 다시 맞으면 **각각 더 큰 값으로 갱신**된다(지속 턴·크기 모두 `max`). 약한 재부여가 기존 강한 상태를 덮어쓰지 않는다.

### 8. 조심할 점
- ⚠️ **로그라이크 '몬스터 행동불가' 선택지는 이 공식을 타지 않는다.** `Unit.ApplyStatus`로 저항 판정 없이 확정 부여한다 — 플레이어가 선택을 소모해 얻은 효과가 몬스터 RES로 헛돌면 선택의 가치가 무너지기 때문.
- 상태이상은 **전투(스테이지) 단위**다. 스테이지를 클리어하면 사라진다.

---

## 공식 이름: 상태이상 스탯 감소 (AtkDown / DefDown / SpdDown)

### 1. 이 공식이 필요한 이유
- 감소를 `Stats`에 직접 적용하면 파티 시너지의 스냅샷 복원과 서로를 덮어쓴다. 그래서 **읽는 시점에 계산**한다.

### 2. 입력값
- 값 1: 원본 스탯 값 (`Stats.Atk` / `Def` / `Spd`)
- 값 2: 해당 상태이상의 `Magnitude` (감소 비율)

### 3. 출력값
- 유효 스탯 (`EffectiveAtk` / `EffectiveDef` / `EffectiveSpd`)

### 4. 공식
```
비율   = 1 − min(1, Magnitude)
유효값 = max(0, Round(원본값 × 비율))
```
- 위치: `Battle/Core/Unit.Reduced`

### 5. 예시 계산
- 입력: Knight DEF 150, Mage 스킬의 DefDown `Magnitude 0.5`
- 계산: `1 − 0.5 = 0.5` → `150 × 0.5 = 75`
- 결과: 유효 DEF **75** (원본 `Stats.Def`는 150 그대로)

### 6. 상한/하한
- 최소값: 0 (Magnitude 1.0 = 해당 스탯 전무)
- 최대값: 원본값 (Magnitude 0)

### 7. 밸런스 의도
- `Magnitude`는 **비율**이라 스탯 스케일의 영향을 받지 않는다. 10배 리스케일 때 스킬 에셋을 건드리지 않아도 된 이유다.

### 8. 조심할 점
- ⚠️ **`Stats`를 직접 고치지 말 것.** 파티 시너지가 이미 `Stats`를 스냅샷/복원 방식으로 조작 중이라 양쪽이 같은 필드를 쓰면 서로를 덮어쓴다.
- `DamageCalculator`와 `TurnOrder`가 이 유효 스탯을 쓴다. 치명타 판정만 원본 `Stats.CritRate`를 본다.

---

## 공식 이름: 도트(중독) 피해

### 1. 이 공식이 필요한 이유
- 고정 피해로 두면 HP가 큰 유닛에게는 무의미하고 작은 유닛에게는 즉사급이 된다. 최대 HP 비율로 두면 어느 유닛에게나 같은 체감이다.

### 2. 입력값
- 값 1: 대상의 `Stats.MaxHp`
- 값 2: 중독의 `Magnitude` (최대 HP 대비 비율)

### 3. 출력값
- 자기 차례 시작 시 받을 피해량

### 4. 공식
```
피해 = max(1, Round(MaxHp × Magnitude, AwayFromZero))
```
- 위치: `Battle/Core/Unit.GetDotDamage`

### 5. 예시 계산
- 입력: Knight MaxHp 1000, Rogue 스킬의 Poison `Magnitude 0.05`
- 계산: `1000 × 0.05 = 50`
- 결과: 턴당 **50** (3턴 지속이면 총 150 = 최대 HP의 15%)

### 6. 상한/하한
- 최소값: 1 (중독이 걸렸는데 피해가 0이 되지 않도록)
- 최대값: 없음 — 최대 HP에 비례

### 7. 밸런스 의도
- **최대 HP 비율이라 탱커에게 더 아프다.** 방어형 캐릭터의 카운터로 기능한다.
- 처리 순서가 곧 규칙이다: 자기 차례 시작 시 **도트 → 지속 턴 감소 → 기절 판정**. 도트를 먼저 넣어야 도트로 죽은 유닛이 그 차례에 행동하지 못한다.

### 8. 조심할 점
- 도트로 죽을 수 있다 — 사망 처리가 행동 전에 일어나야 한다.
- 표시되는 숫자는 데미지 계산과 같은 기준으로 **클램프 전 값**이다.

---

## 공식 이름: 몬스터 디버프 배율 중첩 (다음 스테이지 예약)

### 1. 이 공식이 필요한 이유
- 로그라이크 선택지 중 "몬스터 체력/공격력 감소"는 다음 스테이지 1회에만 적용된다. 여러 번 고르면 곱연산으로 쌓인다.

### 2. 입력값
- 값 1: 선택지의 `EnemyHpMul` / `EnemyAtkMul` (현재 둘 다 0.7)
- 값 2: 이미 예약된 배율

### 3. 출력값
- 스폰 시 몬스터 스탯에 곱해지는 배율 (적용 후 소모)

### 4. 공식
```
예약   : 누적배율 ×= 선택지배율
적용   : 몬스터스탯 = max(1, (int)(스탯 × 누적배율))
소모   : 누적배율 = 1
```
- 위치: `Battle/Core/RunModifiers`

### 5. 예시 계산
- 입력: `EnemyHpDown`(0.7)을 같은 스테이지 보상으로 2회 획득
- 계산: `0.7 × 0.7 = 0.49` → Minion HP 500 → `500 × 0.49 = 245`
- 결과: **245** (51% 감소)

### 6. 상한/하한
- 최소값: 1 (스탯이 0이 되지 않게)
- 최대값: 원본 (배율 1 = 디버프 없음)

### 7. 밸런스 의도
- 일회성이지만 효과가 강해서, **보스 스테이지 직전에 아껴뒀다가 쓰는 전략**이 성립한다.
- 적용 순서는 `기준 스탯 → 스테이지 스케일링 → 디버프`다. 즉 디버프는 이미 성장한 값에 곱해진다.

### 8. 조심할 점
- ⚠️ 스탯 적용은 `Math.Round`가 아니라 **`(int)` 캐스팅(내림)**이다. 다른 공식들과 반올림 방식이 다르다.
- '몬스터 행동불가'만 배율이 아니라 **기절 1턴 확정 부여**로 처리된다(성격이 달라 억지로 통일하지 않음).

---

## 공식 이름: 로그라이크 선택지 등장 가중치

### 1. 이 공식이 필요한 이유
- 선택지 9종이 균등하게 뜨면 영입처럼 상황에 따라 가치가 달라지는 항목이 어색해진다. 또 영구 포인트 투자가 실제 추첨에 반영돼야 "성향 커스터마이징"이 의미를 갖는다.

### 2. 입력값
- 값 1: SO의 기본 가중치 `_weight` (현재 전부 1)
- 값 2: SO의 빈자리 보정 `_weightPerEmptySlot` (영입만 4, 나머지 0)
- 값 3: 파티 빈자리 수, 카테고리별 투자 포인트

### 3. 출력값
- 추첨에 넣을 항목별 가중치

### 4. 공식
```
SO가중치  = max(0, _weight + _weightPerEmptySlot × max(0, 빈자리수))
최종가중치 = SO가중치 + 투자포인트 × _weightPerPoint
```
- 위치: `RoguelikeChoiceSO.GetWeight` + `RoguelikeRewardService.PickChoices`

### 5. 예시 계산
- 입력: 파티 1명(빈자리 3), 영입에 포인트 0 투자
- 계산: 영입 = `1 + 4 × 3 = 13`, 나머지 8종 = 각 `1`
- 결과: 영입 가중치 **13 / 21** — 파티가 빌수록 영입이 자주 뜬다

### 6. 상한/하한
- 최소값: 0 (`Mathf.Max(0f, ...)`)
- 최대값: 없음

### 7. 밸런스 의도
- 영입은 **파티가 꽉 차도 후보에서 빠지지 않는다.** 빈자리 보정이 없어져 상대적으로 드물게 뜰 뿐이며, 뜨면 교체 여부를 플레이어가 고른다.
- 로스터가 비어 있으면 영입 선택지는 아예 제외된다.

### 8. 조심할 점
- 포인트 1점이 가중치 +1이라 **기본 가중치가 1인 현재 값에서는 1점의 영향이 매우 크다**(2배). 기본 가중치를 올리거나 `_weightPerPoint`를 낮추는 쪽으로 조정 여지가 있다.
- 현재 포인트 획득이 **1스테이지당 1점**이라 후반에 포인트가 빠르게 쌓인다.

---

## 공식 이름: 가중치 비례 · 중복 없는 추첨

### 1. 이 공식이 필요한 이유
- 같은 선택지가 3장 중 2장 뜨면 선택지의 의미가 없다. 뽑을 때마다 후보에서 제거하고 남은 것끼리 다시 정규화한다.

### 2. 입력값
- 값 1: 항목별 가중치 목록
- 값 2: 뽑을 개수 (선택지 3, 영입 후보 3)
- 값 3: 난수

### 3. 출력값
- 중복 없는 인덱스 목록

### 4. 공식
```
반복 (뽑은 수 < count 그리고 남은 후보 있음):
    total = Σ max(0, 가중치[남은 후보])
    total ≤ 0 이면 → 균등 추첨
    아니면 roll = rng.Value01() × total 후 누적 차감으로 구간 선택
    선택된 항목을 남은 후보에서 제거
```
- 위치: `Battle/Core/WeightedPicker.PickDistinct`

### 5. 예시 계산
- 입력: 가중치 `[13, 1, 1, 1]`, 2개 추첨
- 계산: 1차 total 16 → 13/16(81%)로 첫 항목. 뽑히면 남은 `[1,1,1]`이 각 1/3로 재정규화
- 결과: 첫 항목이 뽑힌 뒤에는 **나머지가 균등**해진다

### 6. 상한/하한
- 최소값: 빈 리스트 (가중치 없음 / count ≤ 0)
- 최대값: 후보 개수만큼 (count가 후보보다 많아도 넘지 않음)

### 7. 밸런스 의도
- 가중치가 전부 0 이하여도 균등 추첨으로 대체되어 "아무것도 안 뜨는" 상황이 없다.

### 8. 조심할 점
- 부동소수 오차로 끝까지 구간을 못 고르면 **마지막 항목**으로 떨어진다(의도된 폴백).
- 추후 영구 포인트 시스템이 이 위에 얹혀 있으므로 여기를 바꾸면 성향 배분 체감도 함께 바뀐다.

---

## 공식 이름: 영구 포인트 획득 / 잔여

### 1. 이 공식이 필요한 이유
- 런을 넘어 남는 유일한 성장이다. 획득량을 따로 저장하면 최고 기록과 어긋날 수 있어 **`BestStage`에서 파생**시킨다.

### 2. 입력값
- 값 1: 저장된 최고 스테이지 `BestStage`
- 값 2: 몇 스테이지당 1점인지 `stagesPerPoint` (현재 1)
- 값 3: 카테고리별 투자 내역

### 3. 출력값
- 획득 총량 / 투자 합계 / 잔여 포인트

### 4. 공식
```
획득총량 = BestStage / stagesPerPoint      (정수 나눗셈, 내림)
투자합계 = Σ 카테고리별 투자량
잔여     = 획득총량 − 투자합계
```
- 위치: `Progression/Save/SaveData`

### 5. 예시 계산
- 입력: `BestStage 17`, `stagesPerPoint 1`, 공격력에 5점 투자
- 계산: 획득 `17 / 1 = 17` → 잔여 `17 − 5 = 12`
- 결과: 보유 **12 / 총 17**

### 6. 상한/하한
- 최소값: 0 (`stagesPerPoint ≤ 0`이면 0 반환 — 0 나누기 방지)
- 최대값: 없음

### 7. 밸런스 의도
- 획득량을 저장하지 않고 파생시켜 **값이 어긋날 여지 자체를 없앴다.**
- 리스펙이 무료다 — 투자를 되돌리는 데 비용이 없어 실험을 막지 않는다.
- 잔여가 없으면 못 늘리고 투자량이 0이면 못 줄이는 규칙은 **UI가 아니라 `SaveData.TryAdjustPoints`가 판정**한다.

### 8. 조심할 점
- ⚠️ 기획 문서는 "10스테이지마다 1점"인데 **현재 인스펙터 값은 1스테이지당 1점**이다. 의도한 값인지 확인이 필요하다 — 위 선택지 가중치와 맞물려 후반 편중이 커진다.

---

## 공식 이름: 턴 순서 정렬

### 1. 이 공식이 필요한 이유
- 양측 전체를 한 줄로 세워야 하고, SPD가 같을 때 순서가 프레임마다 흔들리면 안 된다.

### 2. 입력값
- 값 1: 생존한 모든 유닛
- 값 2: 각 유닛의 `EffectiveSpd` (상태이상 반영)
- 값 3: 동점 시 `Unit.Id`

### 3. 출력값
- 이번 턴의 행동 순서 (재사용 버퍼)

### 4. 공식
```
1순위: EffectiveSpd 내림차순
2순위: Id 오름차순 (안정적 타이브레이커)
```
- 위치: `Battle/Core/TurnOrder.Build`

### 5. 예시 계산
- 입력: Rogue_Dagger(SPD 150) / Knight(100) / Minion(70)
- 계산: 150 > 100 > 70
- 결과: **Rogue → Knight → Minion**

### 6. 상한/하한
- 최소값: 빈 목록 (생존자 없음)
- 최대값: 생존 유닛 수

### 7. 밸런스 의도
- 매 턴 재정렬하므로 SPD 감소 상태이상이 즉시 반영된다.
- 모든 유닛이 턴당 정확히 1회 행동한다 — **SPD는 "몇 번 행동하는가"가 아니라 "누가 먼저인가"만 결정한다.** SPD 투자 가치를 평가할 때 이 점이 핵심이다.

### 8. 조심할 점
- 반환 리스트는 **재사용 버퍼**다. 턴을 넘겨 보관하면 다음 조회에 덮어써진다.
- 턴 도중 합류한 유닛은 다음 턴부터 순서에 들어온다(순서는 턴 시작 시점의 스냅샷).

---

## 공식 이름: 보스 웨이브 판정

### 1. 이 공식이 필요한 이유
- 무한 타워에 리듬을 준다. 일정 간격마다 확실한 벽이 있어야 "다음 보스까지 버티자"는 목표가 생긴다.

### 2. 입력값
- 값 1: 현재 스테이지
- 값 2: `_bossStageInterval` (현재 5)
- 값 3: 수동 설계 웨이브 개수 (`_monsterWaves`)

### 3. 출력값
- 이번 스테이지에 쓸 `SpawnWaveSO`

### 4. 공식
```
스테이지 ≤ 수동웨이브수 → 수동 설계 웨이브 그대로
그 이후                 → 보스여부 = (스테이지 % _bossStageInterval == 0)
                          보스풀 / 일반풀에서 가중치 추첨
```
- 위치: `Battle/View/MonsterSpawner.ResolveWave` / `PickFromPool`

### 5. 예시 계산
- 입력: 10스테이지, 간격 5, 수동 웨이브 5개
- 계산: `10 > 5` → 랜덤 구간, `10 % 5 == 0` → 보스
- 결과: **보스 풀에서 추첨**

### 6. 상한/하한
- 최소값: 1스테이지
- 최대값: 없음 (무한)

### 7. 밸런스 의도
- 1~5스테이지는 수동 설계로 난이도 곡선을 직접 잡고, 이후는 풀 추첨으로 변주를 만든다.
- 풀이 비어 있으면 기존 수동 웨이브 순환으로 안전하게 폴백한다.

### 8. 조심할 점
- ⚠️ `SpawnWaveSO.IsBossWave`는 **`Tier == Boss`인 몬스터가 있는지**로 판정한다. Elite만으로 구성된 웨이브는 스킬을 써도 보스 웨이브(보스 BGM 포함) 취급이 아니다 — 의도된 동작이다.
- 보스 웨이브 강제는 **랜덤 구간에서만** 작동한다. 수동 웨이브 구간(1~5)에서는 배수여도 수동 설계가 우선한다.

---

## 밸런싱 시 함께 움직여야 하는 값들

| 하나를 바꾸면 | 같이 봐야 하는 것 |
|---|---|
| 스탯 스케일(10배 등) | `DamageCalculator.DefenseConstant` — 안 바꾸면 방어력이 그만큼 세진다 |
| `DefenseConstant` | 모든 캐릭터·몬스터 DEF, 시너지 DEF 비율의 체감 |
| 플레이어 성장률 | 영입 소급 성장(같은 함수를 공유) |
| 몬스터 성장률·복리 | 선택지 수치 — 격차를 선택지가 메우는 설계다 |
| 선택지 기본 가중치 | `_weightPerPoint`(포인트 1점의 상대적 영향력) |
| `_stagesPerPoint` | 후반 선택지 편중 정도 |
| 시너지 수치 | 스탯별 실효 가치가 다르다(ATK 선형 / DEF 체감 / SPD 순서만) |
