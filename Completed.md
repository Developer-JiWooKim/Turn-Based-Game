# 완료 작업 기록

이 문서는 **이미 끝난 작업의 보관소**입니다. 앞으로 할 일은 `TODO.md`, 코드 작업 규칙·현재 코드 상태는 `CLAUDE.md`,
계산식·밸런싱 기준은 `SystemFormulaBalance.md`, 성장 곡선 분석은 `BalanceCurve.md`, UI 비주얼 규칙은 `UI_DesignReference.md`를 참고하세요.

**왜 남기는가** — 여기 적힌 "겪은 함정(⚠️)"과 "원인 분석"이 같은 실수를 반복하지 않게 해줍니다.
설계 판단의 근거(왜 A가 아니라 B를 골랐는가)도 함께 남겨, 나중에 되돌리려 할 때 이유를 다시 발굴하지 않아도 되게 했습니다.

> 코드가 **지금 어떻게 동작하는지**는 이 문서가 아니라 `CLAUDE.md`의 "현재 코드 상태"를 보세요.
> 이 문서는 "언제 무엇을 했고 그때 무엇을 배웠는가"의 기록입니다.

---

## 1. 기반 — 전체 세로 슬라이스 (검증됨)

Intro(타이틀 → 캐릭터 선택) → Battle(전투 → 선택지 → 다음 스테이지) → 전멸 시 결과 → Intro 복귀가 실제 Unity에서 작동함.

- **전투**: Core(턴 순서·데미지·크리·보스 스킬·이벤트) ↔ View(스폰·애니·체력바·클릭 타겟팅·카메라 쉐이크)
- **런/진행**: `RunData`(파티·스테이지), 캐릭터 선택 → 전투 반영, 웨이브별 스테이지 루프, **파티 영입/교체(최대 4명)**, **파티 시너지**(동일 캐릭터 임계 인원 이상 시 전투 중에만 스탯 보너스, 사망으로 조건이 깨지면 즉시 소멸)
- **HUD**: 스테이지·턴 순서·현재 행동 유닛·플레이어 차례 프롬프트·발동 중인 시너지
- **로그라이크 선택지 9종 전부**: 파티 강화 5종(공격력/스피드/방어형/크리/회복) + 몬스터 디버프 3종(행동불가/체력감소/공격력감소, 다음 스테이지 1회 예약) + 파티원 영입(꽉 찼으면 교체 대상 선택)
- **스폰 패턴**: 1~5스테이지 수동 설계 + 6스테이지 이후 랜덤 풀 추첨 + 배수 스테이지 보스 강제
- **결과 화면**: 전멸 시 도달 스테이지 + 최고 기록 팝업(신기록 여부 표시) 후 타이틀 복귀
- **로컬 영구 저장**: `SaveData`/`SaveService`(최고 스테이지·카테고리별 포인트·옵션값을 `save.json`으로 영속화)
- **성향 포인트 배분**: 캐릭터 선택 화면의 '성향' 버튼 → 오버레이 팝업에서 카테고리별(9종) 영구 포인트 투자/리스펙, `RoguelikeRewardService`의 선택지 추첨 가중치에 실시간 반영
- **사운드**: BGM 크로스페이드(씬 전환·전투/보스 전환) + 유닛 전투음/크리티컬/UI클릭·이동·확정/승리·패배 스팅어 + 옵션 팝업 볼륨 슬라이더 3종

---

## 2. 전투 시스템

### 2-1. 보스/엘리트 스킬 실체화 (2026-08-06 이펙트까지 완료)

- 설계 확정(2026-07-23): **Elite=단일 대상 스킬, Boss=전체(라인) 대상 스킬**, Normal은 스킬 없음
- 스킬 전용 애니 클립 연결 완료 — `Skill_SkeletonMage`/`Skill_SkeletonRogue`/`Skill_SkeletonWarrior`가 각 override 컨트롤러에 물려 있음(더 이상 Attack 클립 공유 아님)
- `MonsterStatsSO` 세팅: `MinionSO`=Normal·스킬 없음, `MageSO`/`RogueSO`=Elite·단일 스킬, `WarriorSO`=Boss·전체 스킬
- 스킬 이펙트가 종류별로 갈린다:
  - Skeleton_Mage: `_skillFallsOnTarget` ON → 번개가 대상 머리 위에서 떨어진다
  - Skeleton_Warrior: `_skillMovesToCenter` ON → 전장 한가운데로 나가서 전체 공격
  - Skeleton_Rogue: 평타와 같은 발사 방식에 스킬 전용 투사체만 교체
- 참고: `SpawnWaveSO.IsBossWave`(보스 BGM·보스 웨이브 강제 판정)는 `Tier==Boss`만 보므로, Elite만으로 구성된 웨이브는 스킬을 써도 "보스 웨이브" 취급이 아니다(의도된 동작)

### 2-2. 상태이상 시스템 (2026-08 아이콘 표기까지 완료)

- 계기: `Stats.Res`(디버프 저항)가 선택지·시너지로 값은 쌓이지만 **소비하는 곳이 없어 사실상 죽은 스탯**이었음(2026-07-23 확인) → 이제 RES가 상태이상 저항으로 소비된다
- `StatusKind` 5종(Stun/Poison/AtkDown/DefDown/SpdDown), 부여 정의·진행 상태·저항 판정, 턴 루프 처리(도트→감소→기절), 체력바 표기까지 구현
- **확장성**: 스킬 데이터를 `SkillSO`(유닛 종류에 안 묶인 별도 에셋)로 분리 — 설계 당시에는 캐릭터 스킬 추가(참조 필드 하나 추가)를 확장 지점으로 잡았다
  - ⚠️ **후속: 캐릭터 스킬은 2026-08-19에 범위에서 제외됐다**(`TODO.md`의 '구현하지 않기로 한 것'). 분리 구조는 그대로 두지만 **이 타입의 사용자는 몬스터뿐**이며, 상태이상 코드도 몬스터 시전만 다룬다
- `SkillSO` 에셋 3개 생성 + 몬스터 SO 연결(`ScriptableObjects/Skill/`). 현재 수치(설계 초안에서 밸런싱으로 조정됨):

| 에셋 | 쿨타임 | 범위 | 배율 | 상태이상 |
|---|---|---|---|---|
| `Skill_SkeletonMage` (MageSO) | 5 | Single | 1.4 | 방어력 감소 2턴, 크기 0.5, 확률 0.7 |
| `Skill_SkeletonRogue` (RogueSO) | 5 | Single | 1.1 | 중독 3턴, 크기 0.05, 확률 0.8 |
| `Skill_SkeltonWarrior` (WarriorSO) | 3 | **Line** | 1.8 | 기절 1턴, 확률 0.35 |

- **아이콘 연결(2026-08)**: 태그 조립은 `UnitHealthBar.IconTag(spriteName)` 한 곳에 모였고, 상태이상(`Label`)과 스폰 디버프(`MonsterSpawner.DescribeDebuff`)가 같이 쓴다(크기·기준선이 두 표기에서 어긋나지 않도록)
- **에디터 세팅**: Debuff 아이콘 6종으로 TMP Sprite Asset 개별 생성(`Textures/Icons/Debuff/SpriteAssets/`) → `Debuff_AttackDown.asset`을 `HealthBar.prefab`의 `_statusText`에 할당 → 나머지 5종을 **그 에셋의 Fallback**에 등록. 아이콘을 추가하면 같은 체인의 시작점에 붙일 것(다른 에셋에 걸면 조용히 무시된다)
- ⚠️ **겪은 함정(1d9b459 → 해결)**: `<sprite>`가 인식하는 속성은 name/index/anim/color/tint뿐인데 크기를 `scale=`로 주려다 태그 5종 전부가 문자열로 그대로 출력됐다. 증상이 "스프라이트 이름 못 찾음"과 똑같아 엉뚱한 곳을 오래 뒤졌다 — **크기는 바깥의 `<size=%>`로 준다**. 글자로 보이면 ①잘못된 속성 ②이름 불일치 순으로 의심할 것

### 2-3. 전열/후열 — 어그로 가중 대상 선택 (2026-08-11, 플레이 테스트 확인)

몬스터의 단일 대상 선택이 균등 추첨에서 **어그로 가중 추첨**으로 바뀌었다. 근접(기사·바바리안·도적 단검)이 전열, 원거리 3종이 후열이며 전열이 훨씬 자주 맞는다.

- **화면 배치는 바꾸지 않기로 했다**(2026-08-11 결정) — 슬롯은 가로 일렬 그대로이고 확률만 다르다. 대신 배치로는 드러나지 않으므로 하단 파티 패널에 두 가지를 표기한다:
  - **이름 앞 전열/후열 아이콘**(`Textures/Icons/Position/Front·Back.png`). 이미지 경로는 USS(`.party-row-icon--front/--back`)가 들고 코드는 클래스만 토글한다 — `TitlePanelUI`의 한글 로고 교체와 같은 방식이며, `PartyStatusBarView`는 순수 C# View라 `[SerializeField]`로 스프라이트를 들 수도 없다
  - **어그로 지분(%) 행**. 아이콘만으로는 "얼마나 더 맞는가"가 드러나지 않는다. 분모가 **생존자**의 어그로 합이라 실제 추첨과 같은 기준이고, 누가 쓰러지면 남은 인원의 지분이 함께 올라간다(그래서 `MarkDead`가 `Refresh`를 부른다)
  - ⚠️ 어그로 행은 **맨 뒤**에 둔다 — 앞 7종의 항목·순서가 영입 카드(`RoguelikeRewardService.DescribeStats`)와 맞아야 같은 캐릭터를 두 화면에서 비교할 수 있기 때문. 어그로는 파티 구성에 의존하는 값이라 카드에서는 계산할 수 없어 **여기에만 있다**
- **Core는 가중치(`Unit.AggroWeight`)만 안다.** `BattleRow`는 저작·표기 개념이라 `Game.Data`(`CharacterStatsSO.cs`)에 뒀다 — `MonsterTier`가 `MonsterStatsSO.cs`에 있는 것과 같은 선례이며, 열을 보는 스킬이 생기면 그때 Core로 올린다
- 가중치를 **열이 아니라 캐릭터별로** 둔 이유: 나중에 "기사가 바바리안보다 더 끌어당긴다" 같은 개성을 줄 수 있다. 현재값은 기사 4 / 바바리안·도적(대거) 3 / 후열 3종 1
- **몬스터는 기본값 1을 그대로 쓴다** — 플레이어는 수동 타겟팅이라 AI 추첨을 타지 않아 몬스터 어그로는 읽히지 않는다. 그래서 `MonsterSpawner`는 손대지 않았다
- **후보 목록이 곧 생존자**라 전열이 쓰러지면 남은 유닛끼리 자동으로 재정규화된다(기사 사망 시 바바리안 33%→60%). 별도 처리가 없다
- `WeightedPicker.PickOne` 추가 — **무할당** 단일 추첨. 기존 `PickDistinct`는 호출마다 리스트 둘을 만드는데 대상 추첨은 **모든 몬스터의 모든 행동마다** 불려 무한 타워 내내 할당이 쌓인다(`BattleState` 버퍼 재사용과 같은 이유)
- ⚠️ `_row`와 `_aggroWeight`는 **서로를 강제하지 않는다**(후열인데 가중치를 높게 줘도 컴파일러가 잡지 않는다). 프리팹의 `UnitView._approachTarget`(근접 이동 연출)까지 셋이 짝을 이뤄야 화면과 규칙이 어긋나지 않는다
- ⚠️ 어그로를 올리면 그 캐릭터가 받는 **총 피해량**이 늘어난다 — DEF·HP와 함께 봐야 한다(`SystemFormulaBalance.md`의 "몬스터 대상 선택" 항목)

**도발(Taunt)은 이번 범위가 아니었고, 2026-08-19에 아예 범위에서 제외됐다**(`TODO.md`의 '구현하지 않기로 한 것'). 당시 남겨 뒀던 판단은 이랬다 —
- 끼어들 지점은 `MonsterAiSelector.PickTarget` **하나뿐**이다(시전자를 그대로 반환하면 된다). 그 구조는 지금도 그대로 남아 있다
- **광역기(Line)는 이 함수를 타지 않는다** — 도발과 무관한 기존 구조이므로 도발을 빼도 **되돌릴 것이 없다**
- 구현했다면 `ActiveStatus`에 **시전자 Id 필드**가 필요했다. 제외 결정으로 **그 필드를 추가할 이유가 사라졌다**(지금도 Kind/Magnitude/RemainingTurns뿐)

---

## 3. 전투 연출 (2026-08-06 일괄)

### 3-1. 데미지 숫자 팝업

- `DamagePopup`(팝업 1개의 표현 + 떠오르며 사라지는 연출) + `DamagePopupSpawner`(`ObjectPool` 소유). 일반=흰색 / 크리티컬=빨강+확대 / 도트=별도 색
- `BattlePresenter`의 히트 루프와 `OnStatusTicked` 두 곳에서 스폰하며, **연출 대기(`RegisterPlayback`)에는 넣지 않아** 전투 페이싱은 그대로다
- 위치는 `UnitView.PopupOrigin`(`_popupHeight` 값 하나) — 앵커 오브젝트를 두지 않아 유닛 프리팹 10종을 건드리지 않는다
- ⚠️ 팝업은 **스포너의 자식**이다 — 유닛 View는 풀 반납 시 비활성화되므로 유닛 밑에 두면 재생 중인 팝업이 함께 꺼진다

### 3-2. 타격 순간 연출 시퀀스 (4-1-a)

팝업·피격 연출을 `_impactDelay`(고정값) 대신 **공격 클립의 애니메이션 이벤트** 시점에 맞추고, 그 한 지점에서 타격 파티클·피격 애니메이션·데미지 팝업을 함께 터뜨린다.
*(원래는 히트 스톱도 이 지점에 함께 있었으나 2026-08-11에 제거 — 아래 3-5 참고)*

- **타격 시점**: `UnitAnimator.WaitForImpactAsync(fallback, ct)`가 판단한다. 유닛별 토글 `_useImpactEvent`가 켜져 있으면 클립의 `OnImpactFrame` 이벤트를, 꺼져 있으면 `BattlePresenter._impactDelay`를 쓴다 — 클립 작업을 유닛 하나씩 점진적으로 옮기기 위한 장치
  - ⚠️ 이벤트를 켜 뒀는데 클립에 없으면 전투가 멈추므로 연출 길이를 **안전 타임아웃**으로 두고 풀리면 **경고 로그**를 남긴다(조용히 넘어가지 않는다)
  - ⚠️ 애니메이션 이벤트는 **Animator와 같은 GameObject의 컴포넌트만** 호출한다. `UnitAnimator`가 `[RequireComponent(typeof(Animator))]`로 그 자리에 묶여 있어 성립하는 구조다. 클립이 메서드를 **이름 문자열로** 참조하므로 `OnImpactFrame`을 rename하면 이벤트가 조용히 끊긴다
- **파티클**: `HitEffectSpawner`가 `DamagePopupSpawner`와 같은 풀 구조(단, 프리팹이 여러 종일 수 있어 **프리팹별** 풀). 스크립트 없는 순수 `ParticleSystem`이면 되고 크기·오프셋·수명·크리티컬 배율이 인스펙터 값(수명 0 = 자동 계산)
  - 이펙트·팝업은 `RegisterPlayback`에 넣지 않는다(장식이라 시뮬레이션을 기다리게 할 이유가 없다)
  - 도트 피해에는 붙이지 않았다 — 중독 피해에 물리 타격 스파클은 어울리지 않는다
- **에디터 세팅 완료**: `HitEffectSpawner` 배치 + 슬롯 연결, **유닛 10종 전부 `_useImpactEvent: 1`**이고 실제 재생되는 Attack/Skill 클립에 `OnImpactFrame`이 심겨 있다(`_impactDelay 0.35`는 폴백으로만 남음). 유일한 예외 `Skill_SkeletonMinion.anim`은 Minion이 `Tier=Normal`이라 재생되지 않아 무해

### 3-3. 공격 시 대상 바라보기 + 근접 이동 (4-1-c)

- 공격 전 대상을 향하고 공격 후 제자리로 되돌아온다. **근접·원거리 차이는 `UnitView`가 흡수**하므로 `BattlePresenter`는 구분하지 않는다
  - 근접(`_approachTarget` ON — 바바리안·기사·도적(대거)·스켈레톤 미니언): 대상 앞까지 점프 이동 + 회전
  - 원거리(OFF — 나머지 6종): 제자리 회전만(`_turnDuration` 0.15초)
  - 공격하는 동안 자기 체력바를 숨긴다(양쪽 공통)
- **점프 클립 없이 트랜스폼 포물선**으로 구현 — 애니메이터에 점프 상태가 없기 때문. 클립이 생기면 `MoveAsync`에서 트리거만 같이 재생하면 된다
- 각도 차 5도 미만이면 회전을 건너뛴다 — 정면 대상까지 매번 왕복 회전을 기다리면 페이싱만 쓰게 된다
- 회전은 이동과 같은 구간에 걸쳐 `Slerp`로 함께 돌린다(착지 후 스냅 없음)
- ⚠️ **복귀 판정은 근접 토글이 아니라 `_isDisplaced`(실제로 자리를 떠났는지)**로 한다 — 근접이 아닌 워리어도 전체 공격 연출로 중앙까지 나가기 때문
- ⚠️ 복귀는 `finally`의 `SnapHome()`으로 보장한다 — 취소(씬 종료·배틀 중단)로 왕복이 끊기면 그 유닛만 엉뚱한 자리에 체력바도 없이 선 채 다음 턴이 진행된다
- 참고: 왕복이 연출 대기에 포함돼 **공격 1회당 0.5초**가 페이싱에 더해진다

### 3-4. 투사체 + 스킬별 연출 분기 (4-1-d)

- `ProjectileSpawner` — 발사 → 비행 → 도착. **도착을 기다린 뒤에** 피격 연출이 나간다(화살이 닿기 전에 숫자가 뜨지 않도록)
- 타격 시점의 의미가 원거리에게는 **"명중"이 아니라 "발사"**다. 비행 시간(거리÷속도, 상한 있음)이 그 사이에 들어간다
- 이펙트 3종(`_projectile`/`_muzzleFlash`/`_hitEffect`)은 **유닛별**이고, `_skill*` 3종을 채우면 스킬에만 쓰인다(비우면 평타 것 재사용)
- 발사 지점만 **프리팹 안의 앵커**(`_muzzlePoint`)를 쓴다 — 무기·손 본 아래에 두면 공격 모션을 따라 움직여 실제 무기 위치에서 발사된다(고정 오프셋으로는 불가능)
- 에셋: `MasterStylizedProjectiles`의 `*Bullet`/`*Hit`/`*Muzzle` — 루트에 `ParticleSystem`이 있고 이동 스크립트가 없어 그대로 쓸 수 있다
- **프리팹 배정 완료**: 유닛 10종 전부 `_projectile`·`_muzzlePoint`. 근접 4종도 투사체를 쓰는데, 대상 앞으로 점프한 뒤 짧게 날아가므로 참격 계열 이펙트로 기능한다
- ⚠️ **풀링한 `ParticleSystem`은 `Play()`와 같은 프레임에 `Stop()`하면 입자가 하나도 방출되지 않는다** — 이펙트가 통째로 안 보인다(총구 섬광이 안 보이던 실제 버그)
- ⚠️ **파티클 스케일링 모드를 `Hierarchy`로 덮어써야 한다.** 기본값 `Shape`는 transform 스케일을 방출 모양에만 적용해 크기 배율을 올려도 이펙트가 커지지 않는다. **자식 시스템까지 전부** 바꿀 것

### 3-5. 히트 스톱 제거 (2026-08-11)

플레이 테스트에서 **효과가 전혀 보이지 않아** 도입 5일 만에 걷어냈다. `HitStop.cs`와 그것만을 위해 존재하던 경로(`UnitView.SetAnimationSpeed`, `UnitAnimator.SetSpeedScale`, `ResetToSpawn`의 속도 복구)를 함께 삭제했다.

- **왜 안 보였는가**: `Time.timeScale`을 피하려고 **관여한 유닛 둘의 `Animator.speed`만** 낮추는 구조였다. 화면 전체(카메라·이펙트·다른 유닛)는 그대로 흐르므로, 통상적인 히트 스톱이 주는 "세상이 멎는" 느낌이 나오지 않는다. 기본값(0.08초 × 속도 0.05)이 작아서가 아니라 **범위가 좁아서**라, 값만 키웠다면 공격 모션이 부자연스럽게 끌리기만 했을 것이다
- `Time.timeScale`을 피한 이유 자체는 여전히 유효하다 — 이 프로젝트의 연출 대기와 씬 전환 페이드가 전부 `Awaitable` 기반이라 함께 묶이고, `BattlePausePanel`도 같은 이유로 timeScale을 쓰지 않는다
- **다시 넣는다면**: 같은 구조로는 값 조정이 무의미하므로 **카메라와 이펙트까지 함께 늦추는 방식**을 먼저 설계할 것. 삭제한 구현은 git 이력에 남아 있다
- 남은 자리는 **체력바 감소 애니메이션**(아래 3-6)이 대신한다 — 타격 순간에 눈이 가는 요소가 하나는 있어야 한다

### 3-6. 체력바 감소 애니메이션 (2026-08-11)

`_fill.fillAmount`를 즉시 대입해 게이지가 뚝 끊기던 것을 `_drainDuration`(**0.5초**, 플레이 테스트로 확정)에 걸쳐 미끄러지게 했다. 히트 스톱을 걷어낸 뒤 **타격 순간을 받쳐주는 유일한 장치**라 우선순위가 가장 높았던 항목이다.

- `DamagePopup`·`CameraShake`와 같은 `async Awaitable` 프레임 루프 + `destroyCancellationToken`
- ⚠️ **시간과 곡선이 함께 맞아야 눈에 들어온다.** 처음엔 0.25초 + ease-out(데미지 팝업과 같은 곡선)으로 넣었는데 **즉시 대입과 구분되지 않았다** — ease-out은 변화량의 3/4이 앞 절반에 몰려 "확 줄고 멈춘" 것처럼 보인다. 팝업은 위치 연출이라 튀어나왔다 잦아드는 게 맞지만, **게이지는 줄어드는 과정 자체를 읽혀야 하므로** 양 끝만 눌러 주는 ease-in-out(`Mathf.SmoothStep`)이 맞다. 0.5초 + SmoothStep으로 바꿨다
- **게이지와 숫자를 함께 보간한다** — 같은 값을 보여주는 두 표기가 따로 놀면 어긋난 것처럼 보인다. 다만 같은 정수를 매 프레임 다시 조판하지 않도록 마지막으로 그린 값을 기억해 둔다(유닛마다 TMP 메시 갱신이 쌓이므로)
- ⚠️ **`RegisterPlayback`에 넣지 않는다**(데미지 팝업과 같은 판단) — 장식이라 시뮬레이션을 기다리게 할 이유가 없다
- ⚠️ **풀 재사용 대비로 진입점이 둘이다** — `Set`(연출)과 `SetImmediate`(즉시). `UnitView.Initialize`는 후자를 쓴다. 스폰에 연출을 쓰면 풀에서 물려받은 **이전 전투의 잔량에서 스르륵 차오른다**
- ⚠️ 진행 중인 트윈은 **세대 번호(`_drainVersion`)로 무효화**한다. 새 값이 들어올 때마다 올리고 루프가 매 프레임 확인하므로, 뒤늦게 깨어난 연출이 최신 값을 덮어쓰지 않는다. 체력바가 숨겨져 있으면(공격 중 이동·사망) 애초에 연출 없이 값만 맞춘다

### 4-1. 영입 후보 카드 (2026-08-06 아이콘·스탯 / 2026-08-11 표 정렬)

- `UnitStatsSO`에 `_icon` 추가, 캐릭터 6종 에셋에 아이콘 연결. `ChoiceCard`에 `Icon` 필드, `card-icon` 슬롯(아이콘 없는 카드는 자동 숨김)
- **이름 폰트 축소**: `.card-title` 22px → 16px, 아래 여백 16px → 10px(스탯 줄에 자리를 내주기 위함). 요청은 "절반"이었으나 11px면 `.card-desc`(15px)보다 작아져 제목-본문 위계가 뒤집힌다 — 더 줄이려면 `.card-desc`를 함께 낮춰야 한다
- **스탯 표기 7종**(HP/ATK/SPD/DEF/치명타/치명피해/저항). 배율 3종은 정수 %(치명피해 1.5 → `150%`)이며 **항목·순서·% 표기를 `PartyStatusBarView`와 일부러 맞춰 뒀다** — 같은 캐릭터를 두 화면이 다른 항목 수로 보여주면 비교가 되지 않기 때문
- **2열 표로 정렬(2026-08-11)**: 라벨과 값을 한 문자열로 잇지 않고 `CardStatLine`(라벨+값)으로 나눠, 라벨 폭을 고정해 값이 세로로 줄을 맞춘다(전투 하단 `.party-stat-row`와 같은 구조). 이전에는 `\n`으로 이은 한 덩어리 Label이라 가운데 정렬밖에 되지 않았다
  - ⚠️ `.choice-card`가 `align-items: center`라 `.card-stats`에 `width: 100%`를 줘야 라벨이 카드 왼쪽에서 시작한다
  - ⚠️ 라벨 폭 70px은 영어 "Crit Rate"(한국어 "치명피해"보다 길다) 기준이다 — 더 줄이면 잘린다
- **HP 표기(2026-08-11)**: `800/800`처럼 같은 값이 두 번 찍히던 것을 **현재 HP 한 숫자**로 바꿨다. 영입 후보는 최대치로 합류하므로 그 값이 곧 현재 HP고, 교체 대상은 다친 만큼 낮은 값이 그대로 보인다

### 4-2. 로그라이크 선택지 카테고리 아이콘 (2026-08)

`RoguelikeChoiceSO`에 `_icon` 추가, 9개 에셋에 `Textures/Icons/Buff`/`Debuff` 아이콘 연결(EnemyStun/EnemyHpDown/EnemyAtkDown은 상태이상 Debuff 아이콘과 재사용). 카드 슬롯은 4-1과 공유.

### 4-3. 옵션 메뉴 — 해상도 / 언어 (2026-08-10)

- **해상도**: 화면 모드 프리셋 2종(`창모드 1280×720` / `전체화면 1920×1080`)을 `GameSettings.DisplayPresets`에 두고 버튼으로 고른다. 적용은 `GameSettings.ApplyDisplay()` → `GameManager.Awake` 1회
  - 해상도와 전체화면을 **한 프리셋으로 묶었다** — 따로 두면 "창모드인데 1920×1080" 같은 어긋난 조합이 저장된다. 그래서 `Options.Fullscreen`(bool)은 제거했다(JsonUtility가 없는 필드를 버리므로 기존 세이브와 호환)
  - ⚠️ 프리셋을 늘릴 때는 **배열 뒤에 추가**할 것 — 세이브에 인덱스가 저장되므로 순서를 바꾸면 기존 설정이 다른 모드를 가리킨다
  - ⚠️ 에디터 Game 뷰는 `Screen.SetResolution`을 따르지 않는다 — **확인은 빌드에서**
- **언어(Ko/En)**: 문자열 표(`LocalizationTableSO`) + 조회 진입점(`Loc`)을 만들고 UI 문구 79종을 옮겼다. 언어를 바꾸면 즉시 다시 그려진다(`Loc.LanguageChanged`)
  - **한글 로고 교체**: 이미지 경로는 USS(`.title-logo--ko`)가 들고 `TitlePanelUI`는 클래스만 토글하며, 로고와 그림자를 **함께** 바꾼다(한쪽만 바꾸면 그림자만 영문으로 남는다)
  - ⚠️ **flex 버튼에는 `min-width`가 반드시 필요하다** — Yoga는 CSS의 `min-width: auto`를 적용하지 않아 버튼이 글자보다 좁아지면 텍스트가 밖으로 삐져나온다(영어 "Windowed 1280x720"에서 실제로 겪은 증상)
  - 참고: 월드스페이스 체력바(TMP)는 슬롯 라벨·숫자·아이콘만 찍으므로 **한글 폰트 작업이 필요 없다**
- **에디터 세팅 완료**: 두 씬(Intro/Battle)의 `GameManager._stringTable`에 `UiStringTable.asset` 할당 확인됨

### 4-4. UI 비주얼 톤 교체 (2026-08)

기존 골드/나무 텍스처 카툰 톤을 다크 민트/바이올렛 네온 톤으로 전면 교체(`UI_DesignReference.md`). 버튼(`Common.uss` `.btn` 계열) + 패널·팝업 배경(`.panel-frame`) + 배틀 HUD 턴 순서 칩 모서리·트랜지션까지 적용. *(캐릭터 선택 화면 일부와 버튼 크기는 TODO에 남아 있음)*

### 4-5. 배틀 정보 표시 3종 (2026-08)

- **하단 파티 스탯 표기**: 파티원별 스탯 7종 패널이 그 캐릭터의 **스폰 위치 아래로 정렬**된다(`WorldToScreenPoint` → `RuntimePanelUtils.ScreenToPanel`). 한 행이 `현재값 (+선택지) (+자동성장) (+시너지) (-디버프)` 형태의 색 구분 텍스트이며 0인 항목은 생략
  - ⚠️ **매 프레임 추적하지 않는다** — `CameraShake`가 카메라를 흔들면 하단 바까지 떨린다. 슬롯·카메라가 고정이라 스테이지 시작과 `GeometryChangedEvent`만으로 충분하다
  - ⚠️ 좌표는 `RunData.Members` 순서가 아니라 `UnitId → UnitViewRegistry.TryGet`으로 얻는다(추방·영입으로 두 순서가 갈라지므로)
- **시너지 패널**: 좌상단에 `아이콘 + 이름 + ×요구인원 + 효과` 행 목록. **아직 인원이 모자란 시너지도 흐리게 함께 표시**해 "한 명만 더 모으면 발동"이 보이므로 영입 판단 근거가 된다
  - 부수 수정: `×N`이 현재 인원을 찍어 숫자가 흔들리던 것을 **요구 인원 고정**으로 바로잡음(발동 여부는 농담으로만 구분). `ActiveSynergy` → `PartySynergy`로 rename(미발동도 담게 되어 이름이 거짓이 됨)
- **화면 배치 인덱스**: 체력바 왼쪽에 `[A1]`, 턴 순서 칩에는 이름 대신 `A1`을 찍어 **칩과 화면 위 유닛을 1:1로 맞춘다**(같은 캐릭터를 2명 영입해도 구분된다). 문자열은 `UnitViewRegistry.CreateSlotLabel` 한 곳에서만 만들어 두 표기가 어긋날 수 없다

---

## 5. 입력 (2026-08)

- 바인딩을 `.inputactions` 에셋의 액션맵(Battle/Menu/UI)으로 이전, 코드 하드코딩 제거, 생성 래퍼 삭제
- 키 리바인딩 + 세이브 저장(`Options.InputBindingOverrides`), 옵션 팝업에 키설정 UI
- **키보드 조작**: 배틀 타겟팅(좌/우 방향키 순환=화면 좌→우 정렬, Enter/Space 확정), 캐릭터 선택(방향키=prev/next), 로그라이크 선택지(방향키 카드 겨냥 + Enter/Space 선택). 마우스와 병행하며 각각 조작음 재생
- **에디터 할당 완료**: 두 씬의 InputManager `_actions` 슬롯 연결, 잔여 `PlayerInput` 0건, Missing 스크립트 0건
- ⚠️ **EventSystem의 UI Input Module에서 `m_MoveAction`/`m_SubmitAction`을 끊어 뒀다** — 내장 네비게이션까지 켜져 있으면 방향키가 UI Toolkit 버튼에 포커스를 옮기고 Enter가 그 버튼을 눌러 이중 동작한다(캐릭터 선택에서 방향키→Enter가 prev 버튼을 누르던 버그). 키 조작을 새로 추가할 때 다시 연결하지 말 것
- ⚠️ **액션 경로 문자열의 유일한 출처는 `Systems/InputControls.cs`** — 과거엔 같은 경로가 두 곳에 적혀 있어 한쪽만 갱신하면 **키 설정 UI만 조용히 망가졌다**(버튼에 `-`가 뜨고 눌러도 무반응)

### 배틀 퍼즈 (2026-08)

ESC / HUD 우상단 버튼으로 열고 닫음. 퍼즈 화면에 현재 스테이지·이전 최고 기록 + '계속하기' + '배틀 중단'. **`Time.timeScale`을 쓰지 않고** Core의 `IPauseGate`를 유닛 행동 직전에 await한다(진행 중인 연출을 자르지 않는 턴제다운 경계).

- ⚠️ 퍼즈 해제 시 배틀 입력 복구를 **한 프레임 미룬다** — 오버레이는 3D 레이캐스트를 막지 않아서, 즉시 복구하면 '계속하기' 클릭이 같은 프레임에 타겟팅 클릭으로 새어 들어간다

---

## 6. 진행 / 저장

### 6-1. 이어하기 체크포인트 (2026-08-11)

보스를 클리어할 때마다 런을 `save.json`에 저장하고 타이틀의 '이어하기'로 재개한다. 전멸하면 지우고, '배틀 중단'은 남긴다.

- **저장 시점은 "보스를 막 처치한 직후"**(성장 선택지를 받기 *전*)로 결정했다. 그래서 재개하면 그 보스의 선택지를 다시 뽑는다 — **중단→재개로 선택지를 리롤할 수 있는 것은 의도된 동작**이다
- ⚠️ **재개는 전투가 아니라 승리 후 처리부터 시작한다.** 스냅샷의 의미가 "`CurrentStage` 전투를 이미 이겼다"라서, `BattleDirector`가 스테이지 루프에 들어가기 전에 `AdvanceAfterVictoryAsync`(진급 → 자동 성장 → 선택지)를 한 번 먼저 돌린다. 덕분에 스냅샷에 "선택지 미수령" 같은 플래그가 필요 없다
- ⚠️ **"보스 스테이지" 판정은 `SpawnWaveSO.IsBossWave`**(보스 BGM과 같은 기준)이고 스테이지 번호 배수가 아니다 — 랜덤 풀에 보스 웨이브가 없어 일반 웨이브로 대체된 스테이지까지 세면 안 되기 때문. 반면 `StageScaling`의 SPD 보스 보상은 여전히 **스테이지 번호**에서 파생된다(영입 소급이 되짚을 수 있어야 하므로) — 두 기준이 일부러 다르다
- ⚠️ **캐릭터 식별자는 SO의 에셋 이름(`source.name`)이다.** `DisplayName`은 `Loc.Get`을 거친 **번역문**이라 언어를 바꾸면 값이 달라져 식별자로 쓸 수 없고, 로스터 인덱스는 순서를 바꾸면 조용히 다른 캐릭터가 된다
- `Stats`에 `[Serializable]`을 붙이지 않고 `StatsSnapshot` DTO를 뒀다 — 세이브 포맷이 `Game.Core` 순수 로직 클래스의 필드 배치에 묶이지 않도록. `RunData.UnitIdSeed`도 함께 저장한다(재개 후 발급한 Id가 기존 파티원과 겹치면 `UnitViewRegistry` 조회가 엉킨다)
- 새 런 시작 시 체크포인트가 있으면 `NewRunWarningPopupUI`로 먼저 묻는다. 판정(`SaveService.HasRun`)과 삭제(`ClearRun`)는 팝업이 아니라 `GameUIController`가 하고, 버튼 잠금은 `TitleState.Enter`가 지시한다(패널은 도메인 판정을 갖지 않는다)
- 인스펙터 테스트 파티(BattleScene 단독 실행)로 돌 때는 체크포인트를 저장하지 않는다 — 실제 세이브가 덮여 타이틀에 엉뚱한 이어하기가 뜨지 않도록
- **에디터 씬 배선 + 실기 테스트 완료(2026-08-11)** — IntroScene에 `NewRunWarningPopupUI` 연결 + `GameUIController._newRunWarningPopup` 슬롯 배선. 보스 클리어 → 중단 → 이어하기 → 전멸 → 재확인까지 정상 동작 확인

---

## 7. 구조 / 리팩터링

- **어셈블리 분리(asmdef)**: `Game.Core`→`Data`→`Progression`→`Systems`→`View` 단방향. Core는 `noEngineReferences`라 UnityEngine 참조 시 **컴파일 에러로 차단**된다(Core/View 분리를 규약이 아니라 컴파일러가 강제). Core 유닛 테스트 도입의 전제조건도 함께 해결
- **오브젝트 풀링**: 유닛 View를 파괴하지 않고 프리팹별 `ObjectPool`에 반납·재사용(무한 스테이지 대비). 재사용 시 애니메이터·아웃라인 초기화
  - ⚠️ 렌더러의 "원래 레이어"는 **인스턴스당 1회만** 캐싱한다 — 과거엔 스폰마다 다시 캐싱해서 겨냥 레이어가 원본으로 굳는 함정이 있었다
- **스폰 구조 분리**: 웨이브 선택·몬스터 생성을 `BattleDirector` → `MonsterSpawner`로
- **런 경계 분리**: `BattleRunFlow`(런 시작·종료·세이브·씬 전환) ↔ `BattleDirector`(스테이지 루프)
- **패널 통합**: 배틀 패널 3종을 `BasePanelUI` 상속으로 통합, `PendingSignal<T>`로 TCS 패턴 통합, 시너지 조회 이중 구현 제거
- **성능**: `Camera.main` 캐싱(`MainCameraCache` — 파괴/비활성 시 자동 재조회라 수동 무효화 불필요), 렌더러 1회 캐싱, `BattleState`/`TurnOrder` 버퍼 재사용, 상태이상 이벤트 유닛당 1회
- **널 체크 양식(`Systems/NullCheck`)**: 모든 null 보고를 `[클래스명] 무엇이 어떻다 — 결과(인스펙터 확인).` 형태로 통일. 접두어를 `owner.GetType().Name`에서 뽑아 **복붙으로 다른 클래스 이름이 남는 사고**를 막았다(과거에 실제로 엉뚱한 파일을 뒤졌다)
- **코드 컨벤션 일괄 정리(커밋 `1da61a4`·`15400cd`)**: 한 줄짜리 `if`/`for`도 전부 중괄호로 통일(`CLAUDE.md` 작업 원칙 7)
- **공식 문서화**: 적용된 계산식 14종을 `SystemFormulaBalance.md`에 정리(입력·출력·예시·상한/하한·조심할 점)
- **연출 시퀀스 분리 (2026-08-19)**: `BattlePresenter`(532줄)에서 "행동 1회를 어떻게 보여줄 것인가"를 `ActionPlayback`(순수 C#, 246줄)으로 떼어냈다 — 남은 Presenter는 349줄이고 "Core 이벤트를 화면 어디에 반영할지"만 맡는다. 두 일은 **바뀌는 이유가 다르다**(연출 타이밍 조정 ↔ HUD 표기 변경)
  - ⚠️ **MonoBehaviour로 만들지 않았다.** 연출 참조 5종은 `BattlePresenter`의 `[SerializeField]`에 그대로 두고 `Initialize`에서 생성자로 넘긴다 — 컴포넌트로 쪼갰다면 인스펙터 슬롯이 새 오브젝트로 흩어져 **씬 배선을 다시 해야 했다**. 이 분리는 에디터 작업이 0이다
  - `RegisterPlayback`·`IsPlayingBack`은 옮기지 않았다 — 연출 Task를 **이벤트에 등록**해 시뮬레이션을 대기시키는 것은 구독자의 몫이다
- **패널 폴더 정리 (2026-08-19)**: 배틀 패널 4종을 `Battle/View/Panels/`로 모으고 네임스페이스도 `…Battle.View.Panels`로 맞췄다(`Progression.Run` 선례). `.cs`와 `.cs.meta`를 쌍으로 옮겨 **GUID가 보존되므로 인스펙터 참조가 그대로 살아 있다**
  - 자식 네임스페이스에서 부모(`Battle.View`)는 자동 탐색되므로 패널 쪽에는 using을 추가하지 않았다. 반대 방향 7개 파일에만 `using …Battle.View.Panels;`를 넣었다
- **범위 축소에 따른 정리 (2026-08-19)**: 도발이 빠지며 정당성을 잃은 `MonsterAiSelector.PickTarget`의 미사용 `actor` 인자 제거, 미사용 USS 변수 `--color-cyan-mid`·미사용 아이콘 `Debuff_Taunted.png`·중복 문서 `SystemFormulaBalance_1.md` 삭제
  - 이때 전체 스캔으로 **미사용 `public` 메서드 0개 / 미사용 `[SerializeField]` 0개**를 확인했다 — 죽은 코드가 쌓여 있지 않다는 뜻이라, 이후 정리는 구조 단위로만 하면 된다

---

## 8. 플레이 테스트에서 발견해 고친 것 (2026-08-06)

### 8-1. 교체 대상 카드가 화면 배치 순서와 어긋남

- 증상: 파티가 꽉 찬 상태에서 영입 → 교체 화면에서 **1번 자리가 바바리안인데 1번 카드는 레인저**
- **원인**: 순서의 출처가 둘로 갈라져 있었다 — 화면 순서는 `SpawnMember`가 **앞쪽 빈자리를 재사용**하고, 카드 순서는 `Recruit`이 리스트 **끝에 추가**한다. 사망·추방으로 중간 슬롯이 빈 뒤 영입하면 그때부터 영구히 어긋난다(초기 파티에서는 재현되지 않음)
- **수정**: `UnitViewRegistry.GetPartySlots()`가 슬롯 순서대로 `PartySlot`(멤버 + 배치 라벨)을 돌려주고 카드가 그 순서를 쓴다. 카드 제목에 배치 라벨을 같이 실어(`A1 바바리안`) 순서에 기대지 않고도 짝이 보이게 했다
- ⚠️ 멤버와 라벨을 **한 struct로 묶어** 돌려준 이유: 따로 조회하면 둘이 어긋날 여지가 다시 생긴다
- 선례: `PartyStatusBarView`가 같은 함정을 이미 피해 갔다(좌표를 `UnitId`로 얻는다)

### 8-2. 교체 대상 카드에 아이콘 없음
`PresentReplaceTargetAsync`가 아이콘 인자를 안 넘긴 것. `RunMember.Source`에서 꺼내 넘기도록 수정.

### 8-3. 적 행동 중 '배틀 중단'이 먹지 않는 것처럼 보임

- **원인**: 중단은 `_stageCts.Cancel()`인데 시뮬레이션은 그 순간 `await WhenPlaybackComplete()`에 들어가 있다. 이 대기만 **취소 토큰을 받지 않아** 다음 유닛 행동 직전까지 가서야 중단이 반영된다
- **채택: (A) 버튼을 잠근다.** (B) 즉시 반영은 Core 시그니처를 바꾸고 연출을 중간에 자르게 되므로 "진행 중인 연출은 자르지 않는다"는 기존 설계와 어긋나 채택하지 않았다
- `BattlePresenter.IsPlayingBack`(등록 경로 셋을 한 곳으로 모아 셈, `finally`에서 감소해 취소·예외로도 새지 않음)을 보고 `BattlePausePanel`이 버튼을 잠근다. 퍼즈 중에도 연출은 계속 재생되므로 `Update`에서 매 프레임 확인해 **끝나는 순간 풀린다**
- ⚠️ **조건이 "연출 재생 중"인 것이 핵심**이다 — 플레이어 차례(타겟 입력 대기)와 게이트 대기는 토큰을 받아 즉시 중단되므로 잠그면 안 된다

---

## 9. 밸런싱 조정 이력

### 9-1. 스탯 10배 리스케일 + 시너지 비율화 (2026-08-06)

- 목적은 "숫자가 커 보이게"가 아니라 **반올림 해상도** — ATK가 12이던 시절엔 최소 증가폭 +1이 곧 +8%라 성장이 뭉텅뭉텅 뛰었다
- ⚠️ **`DamageCalculator.DefenseConstant`를 함께 올렸다**(100 → 1000). K는 "피해가 절반으로 줄어드는 DEF 값"이라 스탯만 10배 하면 방어력이 10배 세져 전투가 늘어진다. 다시 스케일을 바꿀 일이 생기면 이 상수를 가장 먼저 볼 것
- **10배 리스케일은 양측을 함께 올렸으므로 상대 밸런스를 바꾸지 않았다** — 몬스터를 여기에 맞춰 더 올리면 리스케일 전보다 어려워진다
- **시너지 혼합 모델**: 정수 스탯(ATK/SPD/DEF)은 비율, 비율 스탯(치명타/치명피해/저항)은 %p 가산. 무한 타워라 고정 증분은 후반에 무의미하지만, 이미 0~1인 비율값에 비율을 곱하면(15%에 +10% → 16.5%) 사실상 아무 일도 안 일어나기 때문

**시너지 1차 조정 결과(효과 기준 +15~18%대로 정렬):**

| 캐릭터 | 값 | 실제 효과 |
|---|---|---|
| Barbarian | ATK +18% | 피해 +18% |
| Rogue_Dagger | 치명타 +20%p | 피해 +17% |
| Mage | 치명피해 +90%p | 피해 +17% |
| Knight | DEF +100%, RES +15%p | 받는 피해 −11.5% (실효 HP +13%) + 저항 |
| Rogue_Crossbow | ATK +12%, SPD +20% | 피해 +12% + 선공 |
| Ranger | SPD +50%, RES +15%p | 선공 + 저항 (유틸리티) |

- ⚠️ 조정 전에는 최상(Barbarian 피해 +30%)과 최하(Ranger 사실상 0)가 3배 넘게 벌어져 있었다. **표시값만 보면 Knight(+53%)·Ranger(+50%)가 과해 보였지만 실제로 과했던 건 Barbarian이다** — 스탯마다 같은 %의 가치가 다르다는 걸 놓치면 이렇게 잘못 짚는다(환산표는 `TODO.md` 밸런싱 항목)

### 9-2. 선택지 계열 간 격차 1차 조정 (2026-08-10)

SpeedUp(Hp 100·Atk 50 추가)·DefensiveUp(Hp 60·Atk 35)·CritUp(치명피해 0.25)을 조정해 50스테이지 전략 간 편차를 **15.5배 → 2.6배**로 좁혔다. AttackUp은 기준선이라 미변경. 부수 효과로 방어 강화의 처치 라운드가 30.1 → 10.0으로 줄어 "전투가 늘어지는" 문제도 해소.

### 9-3. 난이도 곡선 + 속도의 가치 2차 조정 (2026-08-10)

`StageScalingSO`에 필드 5개 추가:

- 30스테이지까지 기존 성장률, 이후 **×1.5 가속**(`_accelStartStage`/`_accelMultiplier`) → 균형 플레이 안전율 S29 `17.4` → S36 `8.6` → S42 `3.7` → S48 `1.6`, 벽은 50 전후. **성장률을 통째로 올리면 초반까지 어려워지므로** 구간을 끊었다
- 5스테이지 이후 **몬스터 SPD 3%/스테이지** → 속도 미투자 시 18스테이지부터 선공 상실(안전율 −38%)
- **보스 처치당 플레이어 SPD +8%** — 완충용이며 몬스터 증가분을 따라잡지 못한다(의도). 같은 속도로 주면 압박이 사라져 "SPD를 올릴 이유가 없다"는 원래 문제로 되돌아간다
- ⚠️ **SPD 스케일링은 원래 배제됐던 결정을 뒤집은 것이다.** 처음엔 "몬스터만 올리면 선공을 계속 뺏겨 체감이 나쁘다"고 제외했는데, 그 결과 속도 강화 선택지가 **빈 선택지**가 됐다. 근거는 `BalanceCurve.md` D-0b절
- ⚠️ **SPD에는 난이도 가속을 적용하지 않는다** — 가속 구간에서 속도까지 튀면 선공이 한순간에 뒤집혀 대응할 여지가 없다
- ⚠️ **난이도를 잴 때 표본 스테이지가 5의 배수(보스)에 몰리지 않게 할 것** — 1차 조정이 실제로 그 오류로 보스전 기준에 맞춰졌다가 재측정으로 바로잡혔다

### 9-4. 선택지 % 전환 + 3차 조정 (2026-08-12)

**전환**: 로그라이크 선택지의 파티 강화 수치를 고정 증분에서 **비율**로 바꿨다. 무한 타워라 "ATK +120"이 후반에 아무것도 바꾸지 못하기 때문이며, 정수 스탯만 비율이고 치명타·치명피해·저항은 %p 가산으로 남겼다(`SynergyBonus`와 같은 혼합 모델). 비율의 기준은 **`Stats − ChoiceGrowth`(= 기준값 + 스테이지 자동 성장)** 라 복리로 부풀지 않는다. 구조·근거는 `CLAUDE.md`의 `RoguelikeEffect` 항목 참고.

**전제 확정**: **4인 파티가 정상 상태**다. `BalanceCurve.md` D-3이 "파티 인원이 난이도를 지배한다"를 문제로 적어뒀지만 의도된 설계로 확정하고, 밸런싱 기준선을 4인으로 옮겼다. ⚠️ `Recruit.asset`의 `_weightPerEmptySlot`는 **난이도 손잡이가 아니라 4인에 빨리 도달시키는 장치**다.

**조정 4건** ("30까지 편하게 / 이후 빠르게" 목표):

| 항목 | 이전 | 이후 | 이유 |
|---|---|---|---|
| `_monsterHpRate` | 0.08 | 0.10 | 후반 처치 라운드를 늘려 벽을 만든다 |
| `_monsterAtkRate` | 0.06 | 0.08 | 안전율이 30까지 **우상향**하던 것을 평평하게 |
| `_accelMultiplier` | 1.5 | 2.0 | 벽 위치 64 → **41스테이지** |
| `DefensiveUp._defRate` | 1.30 | 0.60 | 전략 간 편차 4.05배 → **1.94배** |

결과: 균형 플레이 안전율이 S6~S31 구간 `8.7~10.8`로 평평하고, S34 `4.8` → S38 `1.9` → S42 `0.7`로 꺾인다. 보스는 S35에서 먼저 무너져 실질 종착점이 **35~40 보스**다. 초반(S1~S5)은 거의 변하지 않았다.

- ⚠️ **모델이 두 군데 정확해졌다** — 받는 피해를 `AggroWeight` 가중으로 계산하고(탱커가 실제로 맞아주는 몫), 보스 라인 스킬을 인원수만큼 총량 배가한다. **그래서 이전 절들의 안전율 절대값과 직접 비교하면 안 된다**
- ⚠️ **30 이전은 여전히 1라운드컷이다**(의도적 잔여 문제). 고치려면 몬스터 기준 HP를 올려야 하는데 1~5스테이지 수동 웨이브를 함께 다시 설계해야 한다
- ⚠️ **치명피해가 무한 누적된다**(`+0.25`씩 상한 없음, 24회면 1.5 → 7.5). 비율 전환과 무관한 기존 문제이며 후반 DPS 폭주의 주범이다
- 전체 표와 계산 모델은 `BalanceCurve.md` **F절**

### 9-5. 안전율 모델 폐기 + 캐릭터 기준 스탯 상향 (2026-08-12, 4차 조정)

플레이 테스트에서 "30 도달조차 어렵다"가 나와 모델부터 다시 봤고, **안전율 지표가 틀렸다는 것**을 확인했다.

- ⚠️ **틀린 가정 1 — HP가 스테이지 사이에 회복된다고 봤다.** 실제로는 `RunMember.CurrentHp`가 런 내내 이어지고 돌아오는 건 최대 HP 증가분뿐이라, 진짜 실패 원인은 "한 판에 전멸"이 아니라 **누적 소모**다. 그래서 안전율은 **회복 선택지의 기회비용**을 통째로 놓쳤다
- ⚠️ **틀린 가정 2 — 턴 순서를 무시했다.** "파티 한 라운드 → 몬스터 한 라운드"로 셈했지만 `TurnOrder`는 전원을 SPD로 섞는다. 이 오류가 "30 이전은 1라운드컷"이라는 **오진**을 만들었다(실제로는 평균 1.56라운드)
- 대체 모델은 라운드 단위 시뮬레이터다 — SPD 정렬 행동 순서·집중 공격·어그로 분배·HP 이월·영구 추방·"체력이 낮으면 성장 대신 회복을 고른다"를 반영하고, 지표는 **런 종료 스테이지 / 회복에 쓴 선택지 수 / 최저 멤버 HP 비율**이다

**핵심 결론: 캐릭터 기준 스탯을 올려도 벽은 밀리지 않는다.** 30 이후는 몬스터가 지수로 강해지므로 기준 스탯을 1.5배로 올려도 종료 지점은 35 → 40에서 포화한다. 상향이 바꾸는 건 벽의 위치가 아니라 **거기까지 가는 길의 여유**다.

**적용**: `CharacterStatsSO` 6종 전부 **HP·DEF ×1.5 / ATK ×1.15 / SPD 유지**(치명타·치명피해·저항·시너지는 변경 없음).

- 런 종료 35 → **40**, 회복에 쓴 선택지 **11개 → 6개**, S6~30 평균 라운드 1.88 → 1.56
- ⚠️ **전체 ×1.5는 기각했다** — ATK까지 올리면 전투가 1.08라운드로 줄어 여유가 아니라 **전투의 질감**이 사라진다
- ⚠️ **SPD는 올리지 않았다** — 선공 압박은 의도된 설계다(9-3)
- ⚠️ **`DamageCalculator.DefenseConstant`(1000)는 그대로 뒀다** — 이번 것은 스탯 리스케일이 아니라 플레이어 쪽만 올린 밸런싱이라, K를 함께 올리면 상향이 상쇄된다
- ⚠️ **시뮬레이터도 원하는 카드가 매번 나온다고 가정한다.** 실제로는 9종 중 3장만 뜨고 그중 4종은 영구 성장이 없으므로 **실제 게임은 계산보다 어렵다**

### 9-6. 영입자 부분 소급 (2026-08-12, 5차 조정)

플레이 테스트 보고 "36스테이지쯤 영입하면 공격력이 너무 낮아 후반에 1명이라도 잃으면 무쓸모"를 확인하고, **"선택지 성장은 소급하지 않는다"는 확정 규칙을 부분 소급으로 바꿨다.**

- **원인은 % 전환의 부작용이다.** 비율의 기준이 `기준값 + 자동성장`이라 후반에 고른 선택지 한 장이 초반 것의 2.4배 값어치가 되고, 그만큼 기존 파티원의 누계만 불어났다. 36스테이지 영입자가 기존 파티원 ATK의 **37%(고정 증분 시절) → 25%**로 떨어졌다
- ⚠️ **연쇄 사망이 실측됐다.** 30스테이지에 한 명 잃고 소급 없이 영입한 런에서 34~36 사이에 **다른 파티원이 한 명 더** 죽었다. 약한 영입자가 몫을 못 해 나머지가 더 맞는 구조다
- ⚠️ **자동 성장률 상향은 기각.** `_playerAtkRate`는 영입자·기존 파티원 양쪽에 똑같이 들어가고 선택지 비율의 기준까지 키운다 — 3배로 올려도 영입자 비중은 25% → 37%뿐인데 파티 화력은 2.4배가 되고 벽이 40 → 45로 밀린다
- **적용**: `StageScalingSO._recruitChoiceCatchUpRate` = **0.6**. 현재 파티원들의 `ChoiceGrowth` **평균의 60%**를 물려받는다(스탯 7종 전부 — 치명피해를 빼면 숫자만 맞고 실제 기여가 낮다). 영입자는 베테랑의 약 77%가 되고 연쇄가 끊긴다
- ⚠️ **전부 소급하지는 않는다.** 20스테이지 사망은 60%로도 런이 2스테이지 짧게 끝난다(100%를 줘야 회복) — "파티를 살려두는 게 이득"이라는 설계는 유지된다

**구현에서 걸린 것**

- ⚠️ **소급분은 반드시 `ChoiceGrowth`에 누적한다**(`RunMember.ApplyRecruitCatchUp`). 표시 문제가 아니라 계산상 필수 — `CreateScaleBase`가 `Stats − ChoiceGrowth`로 비율 기준을 만들기 때문에, 넣지 않으면 소급분만큼 기준이 부풀어 **그 멤버만 이후 선택지를 더 받고 기존 파티원을 추월한다**. `ApplyStageGrowth` 경로에 얹어도 같은 문제가 난다(그쪽은 집계하지 않는다)
- ⚠️ **기증자를 만드는 시점** — `Recruit`은 `AddMember` **전에**(새 멤버의 0이 평균을 끌어내린다), `ReplaceMember`는 `Members.Remove` **전에**(교체 대상도 그 성장을 함께 벌어들였다. 빼면 "오래 버틴 멤버를 교체할수록 후임이 약해지는" 반대 결과)
- `RoguelikeEffect`를 재사용하지 않았다 — flat 생성자에 치명타·치명피해·저항 칸이 없다
- **하단 파티 스탯 표기의 서술이 뒤집혔다**: "새 영입자만 파란 `(+선택지)` 괄호가 비어 보이는 것이 정상"이 아니라, 이제는 **채워져 있되 베테랑보다 작은 것**이 정상이다. 과거 "영입 스탯 불일치"로 두 번 의심했던 건의 결론(버그 아님)은 그대로 유효하다
- 전체 표와 근거는 `BalanceCurve.md` **G절**

---

## 10. 종결된 이슈

### 영입 스탯 불일치 의심 → **버그 아님으로 종결 (2026-08)**

영입한 캐릭터가 파티에 이미 있던 같은 캐릭터보다 약한 건 계산 오류가 아니라 **의도된 규칙**이다 — `ApplyCatchUp`은 스테이지 자동 성장을 소급하되 로그라이크 선택지 성장은 일부만 소급한다("그건 그 시점 파티가 벌어들인 몫"이라 파티를 살려두는 데 가치를 두는 설계). 과거에 이걸 두 번 의심했으므로 여기 남긴다.

⚠️ **2026-08-12에 규칙이 한 번 바뀌었다**(9-6). 원래는 선택지 성장을 **전혀** 소급하지 않았고, 그래서 "새 영입자만 파란 `(+선택지)` 괄호가 비어 보이는 것이 정상"이었다. 지금은 파티 누계 평균의 60%를 물려받으므로 **괄호가 채워져 있되 베테랑보다 작은 것**이 정상이다. 종결 판정(버그 아님)은 그대로 유효하다.
